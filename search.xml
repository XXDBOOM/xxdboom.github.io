<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>数据库索引</title>
      <link href="/2026/02/06/%E6%95%B0%E6%8D%AE%E5%BA%93%E7%B4%A2%E5%BC%95/"/>
      <url>/2026/02/06/%E6%95%B0%E6%8D%AE%E5%BA%93%E7%B4%A2%E5%BC%95/</url>
      
        <content type="html"><![CDATA[<h1 id="数据库索引关键知识笔记（学习-面试版）"><a href="#数据库索引关键知识笔记（学习-面试版）" class="headerlink" title="数据库索引关键知识笔记（学习+面试版）"></a>数据库索引关键知识笔记（学习+面试版）</h1><p>核心定位：索引是数据库优化的核心，也是面试高频考点（必考！），笔记聚焦「基础概念+核心原理+面试易错点+实战准则」，兼顾学习理解和面试背诵，适配MySQL、PostgreSQL等主流关系型数据库。</p><h1 id="一、索引基础（必背，面试开篇基础题）"><a href="#一、索引基础（必背，面试开篇基础题）" class="headerlink" title="一、索引基础（必背，面试开篇基础题）"></a>一、索引基础（必背，面试开篇基础题）</h1><h2 id="1-1-索引的定义"><a href="#1-1-索引的定义" class="headerlink" title="1.1 索引的定义"></a>1.1 索引的定义</h2><p>索引（Index）是数据库中用于<em>快速查询、筛选数据</em>的数据结构（本质是“排好序的快速查找数据结构”），相当于书籍的目录——通过目录快速定位到目标章节，无需逐页翻阅，核心作用是「减少磁盘I&#x2F;O，提升查询性能」。</p><p><strong>面试提示</strong>：不要只说“提升查询速度”，要补充“减少磁盘I&#x2F;O”（索引的底层逻辑），体现专业性。</p><h2 id="1-2-索引的核心作用（正反两面，面试常问）"><a href="#1-2-索引的核心作用（正反两面，面试常问）" class="headerlink" title="1.2 索引的核心作用（正反两面，面试常问）"></a>1.2 索引的核心作用（正反两面，面试常问）</h2><ul><li><strong>正向作用（核心）</strong>：① 加速查询（where筛选、join关联、order by排序、group by分组）；② 减少全表扫描，降低磁盘I&#x2F;O开销；③ 保证数据唯一性（唯一索引）。</li><li><strong>反向代价（易错点）</strong>：① 占用磁盘空间（索引是独立的数据结构，不是免费的）；② 降低写入性能（增&#x2F;删&#x2F;改操作时，需同步维护索引树，增加额外开销）—— 索引本质是“以写换读”。</li></ul><p><strong>面试考点</strong>：“为什么不给所有字段加索引？” → 答：索引会占用磁盘空间，且写入时需维护索引，过度索引会导致写入性能暴跌。</p><h2 id="1-3-索引的核心原则（底层判断依据）"><a href="#1-3-索引的核心原则（底层判断依据）" class="headerlink" title="1.3 索引的核心原则（底层判断依据）"></a>1.3 索引的核心原则（底层判断依据）</h2><p>索引的价值取决于「字段区分度（基数）」，公式：<code>区分度 = 字段唯一值数量 / 表总行数</code></p><ul><li>高区分度（≈1）：适合建索引（如id、手机号、订单号），能快速筛选少量数据；</li><li>低区分度（&lt;0.01）：谨慎建索引（如状态字段、逻辑删除字段），筛选数据过多时，索引会失效。</li></ul><h1 id="二、索引分类（核心重点，面试高频区分题）"><a href="#二、索引分类（核心重点，面试高频区分题）" class="headerlink" title="二、索引分类（核心重点，面试高频区分题）"></a>二、索引分类（核心重点，面试高频区分题）</h1><p>按「功能+结构」分类，重点掌握前4类，其余了解即可，面试常考“聚簇vs非聚簇”“唯一vs普通”的区别。</p><h2 id="2-1-按功能分类（实战常用，必背）"><a href="#2-1-按功能分类（实战常用，必背）" class="headerlink" title="2.1 按功能分类（实战常用，必背）"></a>2.1 按功能分类（实战常用，必背）</h2><table><thead><tr><th align="left">索引类型</th><th align="left">核心特点</th><th align="left">适用场景</th><th align="left">面试提示</th></tr></thead><tbody><tr><td align="left">主键索引（PRIMARY KEY）</td><td align="left">① 唯一且非空；② 一张表只能有1个；③ 默认是聚簇索引（MySQL）</td><td align="left">所有业务表（唯一标识一行数据）</td><td align="left">必考：“主键索引和普通索引的区别？”</td></tr><tr><td align="left">唯一索引（UNIQUE KEY）</td><td align="left">① 唯一，允许NULL（多个NULL不冲突）；② 可建多个</td><td align="left">手机号、邮箱、订单号等需唯一的字段</td><td align="left">易错：唯一索引≠主键索引，可多个，允许NULL</td></tr><tr><td align="left">普通索引（INDEX）</td><td align="left">① 不唯一、允许NULL；② 可建多个；③ 最常用</td><td align="left">高频查询的非唯一字段（如商品分类、用户名模糊查询）</td><td align="left">注意：低区分度字段（如status）不建议建单列普通索引</td></tr><tr><td align="left">组合索引（联合索引）</td><td align="left">① 多个字段组合建立的索引；② 遵循“最左前缀原则”</td><td align="left">多字段联合查询（如where status&#x3D;1 and create_time&gt;’2026-01-01’）</td><td align="left">面试必考：最左前缀原则、组合索引的设计顺序</td></tr><tr><td align="left">覆盖索引</td><td align="left">非独立索引类型，索引包含查询所需所有字段，无需回表</td><td align="left">高频简单查询（如select id,status from order where status&#x3D;0）</td><td align="left">核心：减少回表开销，提升查询性能</td></tr></tbody></table><h2 id="2-2-按结构分类（底层原理，面试必问）"><a href="#2-2-按结构分类（底层原理，面试必问）" class="headerlink" title="2.2 按结构分类（底层原理，面试必问）"></a>2.2 按结构分类（底层原理，面试必问）</h2><h3 id="✅-聚簇索引（Clustered-Index）"><a href="#✅-聚簇索引（Clustered-Index）" class="headerlink" title="✅ 聚簇索引（Clustered Index）"></a>✅ 聚簇索引（Clustered Index）</h3><ul><li>核心：<strong>索引结构和数据本身存储在一起</strong>，索引的叶子节点就是数据行（MySQL中，主键索引默认是聚簇索引）。</li><li>优势：查询速度极快，无需回表（找到索引就是找到数据）；</li><li>劣势：① 一张表只能有1个聚簇索引；② 插入&#x2F;删除时，需移动数据，维护成本高；③ 主键无序（如UUID）会导致索引碎片。</li></ul><h3 id="✅-非聚簇索引（Non-Clustered-Index）"><a href="#✅-非聚簇索引（Non-Clustered-Index）" class="headerlink" title="✅ 非聚簇索引（Non-Clustered Index）"></a>✅ 非聚簇索引（Non-Clustered Index）</h3><ul><li>核心：<strong>索引结构和数据分开存储</strong>，索引的叶子节点存储的是「主键值」，需通过主键值回表查询具体数据（又称“二级索引”）。</li><li>优势：① 可建多个；② 插入&#x2F;删除维护成本低，不影响数据本身；</li><li>劣势：查询时可能需要回表（除非是覆盖索引），性能略低于聚簇索引。</li></ul><p><strong>面试高频题</strong>：聚簇索引和非聚簇索引的区别？（直接背以下3点）</p><ol><li>存储方式：聚簇索引“索引+数据”共存，非聚簇索引“索引和数据分离”；</li><li>数量限制：聚簇索引最多1个，非聚簇索引可多个；</li><li>查询性能：聚簇索引无需回表（更快），非聚簇索引需回表（除非覆盖索引）。</li></ol><h2 id="2-3-其他索引（了解即可，面试偶尔问）"><a href="#2-3-其他索引（了解即可，面试偶尔问）" class="headerlink" title="2.3 其他索引（了解即可，面试偶尔问）"></a>2.3 其他索引（了解即可，面试偶尔问）</h2><ul><li>哈希索引：基于哈希表，查询速度极快（O(1)），但不支持排序、范围查询，MySQL的Memory引擎支持；</li><li>全文索引：用于文本模糊查询（如文章内容搜索），MySQL5.6+支持，性能不如Elasticsearch，仅适用于简单文本查询；</li><li>空间索引：用于地理空间数据查询（如地图坐标），实际开发中极少用。</li></ul><h1 id="三、索引底层数据结构（面试核心难点，必理解-背诵）"><a href="#三、索引底层数据结构（面试核心难点，必理解-背诵）" class="headerlink" title="三、索引底层数据结构（面试核心难点，必理解+背诵）"></a>三、索引底层数据结构（面试核心难点，必理解+背诵）</h1><p>主流数据库（MySQL、PostgreSQL）的索引底层都是「B+树」，面试必问“为什么用B+树，不用B树、哈希表？”，重点理解B+树的优势。</p><h2 id="3-1-核心数据结构：B-树（重点）"><a href="#3-1-核心数据结构：B-树（重点）" class="headerlink" title="3.1 核心数据结构：B+树（重点）"></a>3.1 核心数据结构：B+树（重点）</h2><h3 id="B-树的结构特点（简化理解，无需记复杂节点细节）"><a href="#B-树的结构特点（简化理解，无需记复杂节点细节）" class="headerlink" title="B+树的结构特点（简化理解，无需记复杂节点细节）"></a>B+树的结构特点（简化理解，无需记复杂节点细节）</h3><ul><li>是「平衡多路查找树」，避免树的高度过高（减少磁盘I&#x2F;O次数）；</li><li>叶子节点：按顺序排列，存储数据（聚簇索引）或主键值（非聚簇索引），叶子节点之间用指针连接（支持范围查询）；</li><li>非叶子节点：仅存储索引关键字，不存储数据，用于快速定位叶子节点。</li></ul><h3 id="B-树的核心优势（面试必背，4点）"><a href="#B-树的核心优势（面试必背，4点）" class="headerlink" title="B+树的核心优势（面试必背，4点）"></a>B+树的核心优势（面试必背，4点）</h3><ol><li>「高度低，I&#x2F;O少」：多路查找树，树的高度通常是3-4层（百万级数据，3层即可覆盖），每次查询只需3-4次磁盘I&#x2F;O，速度极快；</li><li>「支持范围查询」：叶子节点有序且用指针连接，无需回溯，范围查询（如between、&gt;、&lt;）效率极高；</li><li>「查询稳定」：所有查询最终都要落到叶子节点，查询时间固定，不会出现极端慢查询；</li><li>「存储效率高」：非叶子节点不存储数据，仅存索引关键字，单个节点能存储更多关键字，进一步降低树的高度。</li></ol><h2 id="3-2-对比其他结构（面试必问，突出B-树的优势）"><a href="#3-2-对比其他结构（面试必问，突出B-树的优势）" class="headerlink" title="3.2 对比其他结构（面试必问，突出B+树的优势）"></a>3.2 对比其他结构（面试必问，突出B+树的优势）</h2><table><thead><tr><th align="left">数据结构</th><th align="left">优势</th><th align="left">劣势</th><th align="left">为什么不用？</th></tr></thead><tbody><tr><td align="left">B树</td><td align="left">平衡多路查找树，I&#x2F;O较少</td><td align="left">① 非叶子节点存储数据，存储效率低，树高更高；② 叶子节点无序，不支持范围查询；③ 查询不稳定（可能在非叶子节点找到数据）</td><td align="left">范围查询差，存储效率低，不如B+树适配数据库场景</td></tr><tr><td align="left">哈希表</td><td align="left">单值查询极快（O(1)）</td><td align="left">① 不支持范围查询、排序；② 哈希冲突需处理；③ 大量数据时，性能下降明显</td><td align="left">数据库高频场景（范围查询、排序）不支持，实用性低</td></tr><tr><td align="left">红黑树</td><td align="left">平衡二叉树，查询、插入、删除效率高</td><td align="left">二叉树，树高过高（百万级数据，树高约20层），磁盘I&#x2F;O次数过多</td><td align="left">树高过高，I&#x2F;O开销大，不适合大数据量场景</td></tr></tbody></table><p><strong>面试话术</strong>：“MySQL索引底层用B+树，核心原因是B+树高度低、I&#x2F;O少，支持范围查询和排序，查询稳定，能完美适配数据库的高频查询场景（单值、范围、排序），而B树、哈希表、红黑树都存在明显劣势，无法满足数据库的性能需求。”</p><h1 id="四、索引设计准则（实战-面试，必背，避坑重点）"><a href="#四、索引设计准则（实战-面试，必背，避坑重点）" class="headerlink" title="四、索引设计准则（实战+面试，必背，避坑重点）"></a>四、索引设计准则（实战+面试，必背，避坑重点）</h1><p>索引设计的核心：「精准匹配业务查询，拒绝过度索引」，按优先级设计，遵循以下准则，能避免95%的索引失效和性能问题。</p><h2 id="4-1-索引创建优先级（面试必背，口诀记牢）"><a href="#4-1-索引创建优先级（面试必背，口诀记牢）" class="headerlink" title="4.1 索引创建优先级（面试必背，口诀记牢）"></a>4.1 索引创建优先级（面试必背，口诀记牢）</h2><p><strong>口诀</strong>：主键必建第一流，唯一索引排前头；组合索引是核心，单列索引兜底用；覆盖索引做优化，过度索引不可留。</p><ol><li>第一优先级：主键索引（所有表必须有，优先用bigint自增ID，避免UUID）；</li><li>第二优先级：唯一索引（业务唯一字段，如手机号、订单号，兼顾唯一性和查询性能）；</li><li>第三优先级：组合索引（高频多字段联合查询，解决低区分度字段索引失效问题）；</li><li>第四优先级：单列普通索引（仅单一字段高频查询，无组合条件时用）；</li><li>第五优先级：覆盖索引（基于已有索引优化，无需新建，减少回表）。</li></ol><h2 id="4-2-状态字段索引设计（高频场景，面试延伸题）"><a href="#4-2-状态字段索引设计（高频场景，面试延伸题）" class="headerlink" title="4.2 状态字段索引设计（高频场景，面试延伸题）"></a>4.2 状态字段索引设计（高频场景，面试延伸题）</h2><p>状态字段（status、del_flag等）是低区分度字段，设计索引时需注意：</p><ul><li>✅ 必加索引的场景：高频查询、筛选后返回少量数据（如where status&#x3D;0，筛选出1%数据）；</li><li>❌ 不加索引的场景：低频查询、筛选后返回70%以上数据（如del_flag&#x3D;0占99.9%）；</li><li>⚠️ 核心优化：状态字段<strong>不建议建单列索引</strong>，优先和其他字段建组合索引（如idx_del_flag_status(del_flag, status)）。</li></ul><h2 id="4-3-索引设计避坑准则（易错点，面试常考）"><a href="#4-3-索引设计避坑准则（易错点，面试常考）" class="headerlink" title="4.3 索引设计避坑准则（易错点，面试常考）"></a>4.3 索引设计避坑准则（易错点，面试常考）</h2><ul><li>❌ 不给低区分度+高频筛选+数据分布极端的字段建单列索引（如del_flag，99.9%是0）；</li><li>❌ 不建过度索引（单表索引≤5个，最多不超过8个）；</li><li>❌ 不给更新频率远大于查询频率的字段建索引（如实时更新的支付状态，写入多、查询少）；</li><li>✅ 组合索引设计：高频筛选字段放左边，区分度高的字段放左边（如idx_status_createtime(status, create_time)）；</li><li>✅ 优先用数值类型建索引（如status用tinyint，不用varchar），避免隐式类型转换；</li><li>✅ 避免用NULL值（NULL会导致索引失效，可设置默认值，如status默认0）。</li></ul><h1 id="五、索引失效场景（面试重中之重，必背-举例）"><a href="#五、索引失效场景（面试重中之重，必背-举例）" class="headerlink" title="五、索引失效场景（面试重中之重，必背+举例）"></a>五、索引失效场景（面试重中之重，必背+举例）</h1><p>索引失效是面试高频题，常考“以下SQL为什么不走索引？”，核心记住：「只要破坏索引的查询逻辑，就会失效」，重点场景如下（带SQL示例，方便理解）。</p><h2 id="5-1-核心失效场景（必背，8个）"><a href="#5-1-核心失效场景（必背，8个）" class="headerlink" title="5.1 核心失效场景（必背，8个）"></a>5.1 核心失效场景（必背，8个）</h2><ol><li><strong>对索引字段做函数运算</strong>（最常见）         示例：<code>select * from user where date(create_time) = &#39;2026-01-01&#39;</code> 原因：函数运算会破坏索引的有序性，数据库无法走索引，需优化为：<code>select * from user where create_time between &#39;2026-01-01 00:00:00&#39; and &#39;2026-01-01 23:59:59&#39;</code></li><li><strong>索引字段发生隐式类型转换</strong> 示例：<code>select * from user where status = &#39;1&#39;</code>（status是tinyint类型，传入字符串）         原因：数据库会自动转换类型，等价于<code>cast(status as char) = &#39;1&#39;</code>，破坏索引有序性。      </li><li><strong>使用不等于（!&#x3D;、&lt;&gt;）、not in、is not null</strong> 示例：<code>select * from user where status != 1</code> 原因：这类查询会筛选出大部分数据，数据库认为走索引开销大于全表扫描，直接放弃索引。      </li><li><strong>模糊查询以%开头</strong> 示例：<code>select * from user where name like &#39;%张三&#39;</code> 原因：%开头无法利用索引的有序性，无法快速定位，若必须模糊查询，可用覆盖索引或全文索引。         ✅ 有效写法：<code>select * from user where name like &#39;张三%&#39;</code>（%结尾，可走索引）      </li><li><strong>违反组合索引的最左前缀原则</strong> 示例：组合索引<code>idx_a_b_c(a,b,c)</code>，查询<code>select * from user where b=2 and c=3</code> 原因：最左前缀原则要求“从左到右匹配索引字段”，缺少a字段，索引完全失效。      </li><li><strong>or连接非索引字段</strong> 示例：<code>select * from user where status=1 or age=20</code>（age无索引）         原因：or连接的字段中，有一个无索引，数据库会放弃所有索引，走全表扫描。      </li><li><strong>索引字段数据分布极端（低区分度+大部分数据符合条件）</strong> 示例：<code>select * from user where del_flag=0</code>（del_flag&#x3D;0占99.9%）         原因：走索引的开销＞全表扫描，数据库自动选择全表扫描。      </li><li><strong>使用order by非索引字段</strong> 示例：<code>select * from user where status=1 order by age</code>（age无索引）         原因：非索引字段排序，需进行文件排序（filesort），效率极低，且会导致索引失效。</li></ol><h2 id="5-2-索引失效排查技巧（面试延伸，体现实战能力）"><a href="#5-2-索引失效排查技巧（面试延伸，体现实战能力）" class="headerlink" title="5.2 索引失效排查技巧（面试延伸，体现实战能力）"></a>5.2 索引失效排查技巧（面试延伸，体现实战能力）</h2><p>实际开发中，用MySQL的<code>explain</code>命令排查索引是否生效：</p><ul><li>看<code>type</code>字段：<code>all</code>（全表扫描，索引失效）、<code>ref</code>&#x2F;<code>range</code>&#x2F;<code>eq_ref</code>（索引生效）；</li><li>看<code>key</code>字段：显示NULL，说明索引失效；显示具体索引名，说明走了对应索引；</li><li>看<code>Extra</code>字段：出现<code>Using filesort</code>（排序失效）、<code>Using where; Using filesort</code>（索引失效+排序失效）。</li></ul><h1 id="六、面试高频真题（直接背诵，适配面试场景）"><a href="#六、面试高频真题（直接背诵，适配面试场景）" class="headerlink" title="六、面试高频真题（直接背诵，适配面试场景）"></a>六、面试高频真题（直接背诵，适配面试场景）</h1><p>以下真题是大厂面试高频题，整理好标准答案，直接背诵，避免面试卡顿。</p><h2 id="真题1：什么是索引？索引的作用和代价分别是什么？"><a href="#真题1：什么是索引？索引的作用和代价分别是什么？" class="headerlink" title="真题1：什么是索引？索引的作用和代价分别是什么？"></a>真题1：什么是索引？索引的作用和代价分别是什么？</h2><p>答：索引是数据库中用于快速查询、筛选数据的数据结构，本质是排好序的快速查找结构，相当于书籍目录。 作用：① 加速查询，减少磁盘I&#x2F;O；② 保证数据唯一性（唯一索引）。 代价：① 占用磁盘空间；② 降低写入性能（增删改需维护索引树），索引是“以写换读”。</p><h2 id="真题2：聚簇索引和非聚簇索引的区别？MySQL中默认是什么索引？"><a href="#真题2：聚簇索引和非聚簇索引的区别？MySQL中默认是什么索引？" class="headerlink" title="真题2：聚簇索引和非聚簇索引的区别？MySQL中默认是什么索引？"></a>真题2：聚簇索引和非聚簇索引的区别？MySQL中默认是什么索引？</h2><p>答：区别有3点：① 存储方式：聚簇索引“索引+数据”共存，非聚簇索引分离；② 数量限制：聚簇索引最多1个，非聚簇索引可多个；③ 查询性能：聚簇索引无需回表，非聚簇索引需回表（除非覆盖索引）。 MySQL中，主键索引默认是聚簇索引，若无主键，会选择唯一非空索引作为聚簇索引，若都没有，会自动生成隐藏聚簇索引。</p><h2 id="真题3：MySQL索引底层为什么用B-树，不用B树、哈希表？"><a href="#真题3：MySQL索引底层为什么用B-树，不用B树、哈希表？" class="headerlink" title="真题3：MySQL索引底层为什么用B+树，不用B树、哈希表？"></a>真题3：MySQL索引底层为什么用B+树，不用B树、哈希表？</h2><p>答：核心原因是B+树能完美适配数据库的高频查询场景，优势如下：① 高度低，I&#x2F;O少，3-4层即可覆盖百万级数据，查询速度快；② 叶子节点有序且连表，支持范围查询和排序；③ 查询稳定，所有查询都落到叶子节点；④ 非叶子节点不存数据，存储效率高。 不用B树：B树非叶子节点存数据，存储效率低，不支持范围查询； 不用哈希表：哈希表不支持范围查询、排序，哈希冲突需处理，大数据量性能下降。</p><h2 id="真题4：什么情况下索引会失效？举3个常见例子。"><a href="#真题4：什么情况下索引会失效？举3个常见例子。" class="headerlink" title="真题4：什么情况下索引会失效？举3个常见例子。"></a>真题4：什么情况下索引会失效？举3个常见例子。</h2><p>答：破坏索引查询逻辑，就会导致索引失效，常见例子：① 对索引字段做函数运算（如date(create_time)&#x3D;’2026-01-01’）；② 模糊查询以%开头（如name like ‘%张三’）；③ 违反组合索引最左前缀原则（如组合索引idx_a_b，查询where b&#x3D;2）；④ 索引字段隐式类型转换（如status&#x3D;’1’，status是tinyint）。</p><h2 id="真题5：如何设计高效的索引？状态字段（如status）如何建索引？"><a href="#真题5：如何设计高效的索引？状态字段（如status）如何建索引？" class="headerlink" title="真题5：如何设计高效的索引？状态字段（如status）如何建索引？"></a>真题5：如何设计高效的索引？状态字段（如status）如何建索引？</h2><p>答：高效索引设计遵循优先级：主键索引→唯一索引→组合索引→单列普通索引，核心准则是“精准匹配业务查询，拒绝过度索引”，还要注意：① 高频查询字段优先建索引；② 组合索引高频字段、高区分度字段放左边；③ 避免过度索引（单表≤5个）；④ 数值类型优先建索引。 状态字段（低区分度）：不建议建单列索引，若高频查询，优先和其他字段建组合索引（如idx_status_createtime(status, create_time)）；若筛选后返回大部分数据，不建索引。</p><h2 id="真题6：唯一索引和主键索引的区别？"><a href="#真题6：唯一索引和主键索引的区别？" class="headerlink" title="真题6：唯一索引和主键索引的区别？"></a>真题6：唯一索引和主键索引的区别？</h2><p>答：① 唯一性：两者都保证唯一，但主键索引非空，唯一索引允许NULL；② 数量限制：主键索引只能有1个，唯一索引可建多个；③ 存储方式：主键索引默认是聚簇索引，唯一索引是非聚簇索引（除非无主键，唯一非空索引成为聚簇索引）。</p><h1 id="七、总结（核心背诵口诀，快速回顾）"><a href="#七、总结（核心背诵口诀，快速回顾）" class="headerlink" title="七、总结（核心背诵口诀，快速回顾）"></a>七、总结（核心背诵口诀，快速回顾）</h1><h2 id="核心口诀（面试前快速过一遍）"><a href="#核心口诀（面试前快速过一遍）" class="headerlink" title="核心口诀（面试前快速过一遍）"></a>核心口诀（面试前快速过一遍）</h2><ol><li>索引本质：排好序的目录，以写换读，减少I&#x2F;O；</li><li>分类口诀：主键唯一组合列，聚簇非聚分清楚；覆盖索引提性能，哈希全文了解足；</li><li>底层口诀：B+树是核心，高度低、支持范围，查询稳定效率高；</li><li>设计口诀：主键必建，唯一优先，组合核心，单列兜底，过度不行；</li><li>失效口诀：函数转换不等，%前or非索引，最左前缀别违反，数据极端也失效。</li></ol><h2 id="面试提示"><a href="#面试提示" class="headerlink" title="面试提示"></a>面试提示</h2><p>索引面试重点：底层B+树、聚簇vs非聚簇、索引失效、设计准则，这4个部分占90%的考点，优先背诵；其他知识点（如哈希索引、空间索引）了解即可，无需深入。回答问题时，结合SQL示例，体现实战能力，会更加分。</p>]]></content>
      
      
      
        <tags>
            
            <tag> db </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Java 并发编程核心知识点笔记</title>
      <link href="/2026/02/05/Java-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E6%A0%B8%E5%BF%83%E7%9F%A5%E8%AF%86%E7%82%B9%E7%AC%94%E8%AE%B0/"/>
      <url>/2026/02/05/Java-%E5%B9%B6%E5%8F%91%E7%BC%96%E7%A8%8B%E6%A0%B8%E5%BF%83%E7%9F%A5%E8%AF%86%E7%82%B9%E7%AC%94%E8%AE%B0/</url>
      
        <content type="html"><![CDATA[<h1 id="Java并发编程核心知识点笔记"><a href="#Java并发编程核心知识点笔记" class="headerlink" title="Java并发编程核心知识点笔记"></a>Java并发编程核心知识点笔记</h1><h2 id="（线程池-异步回调-synchronized-x2F-volatile-整合版）"><a href="#（线程池-异步回调-synchronized-x2F-volatile-整合版）" class="headerlink" title="（线程池 | 异步回调 | synchronized&#x2F;volatile 整合版）"></a>（线程池 | 异步回调 | synchronized&#x2F;volatile 整合版）</h2><p>本文整合线程池核心原理、无&#x2F;有参异步回调、Java同步关键字的核心知识点，搭配可运行示例，用于本地温习，覆盖日常开发与面试高频考点。</p><hr><h2 id="一、Java线程池（SpringBoot-实践）"><a href="#一、Java线程池（SpringBoot-实践）" class="headerlink" title="一、Java线程池（SpringBoot 实践）"></a>一、Java线程池（SpringBoot 实践）</h2><p>线程池是管理线程的核心工具，避免频繁创建&#x2F;销毁线程，提升并发性能，核心遵循「核心线程→队列→最大线程→拒绝策略」的工作流程。</p><h3 id="1-1-核心参数与工作流程"><a href="#1-1-核心参数与工作流程" class="headerlink" title="1.1 核心参数与工作流程"></a>1.1 核心参数与工作流程</h3><table><thead><tr><th>参数名</th><th>含义</th><th>核心作用</th></tr></thead><tbody><tr><td>corePoolSize</td><td>核心线程数（长期存活，默认不销毁）</td><td>线程池基础处理能力</td></tr><tr><td>maximumPoolSize</td><td>最大线程数（核心+非核心线程总和）</td><td>并发处理能力上限</td></tr><tr><td>queueCapacity</td><td>任务队列容量（存储等待执行的任务）</td><td>缓冲突发流量，避免频繁创建线程</td></tr><tr><td>keepAliveTime</td><td>非核心线程空闲超时时间（超时自动销毁）</td><td>释放空闲资源</td></tr><tr><td>rejectedExecutionHandler</td><td>拒绝策略（线程池满载时的兜底方案）</td><td>保护系统不被压垮</td></tr></tbody></table><p><strong>工作流程</strong>：<br>提交任务 → 核心线程未满？创建核心线程 → 核心线程满？任务入队列 → 队列满？创建非核心线程 → 最大线程满？触发拒绝策略。</p><h3 id="1-2-SpringBoot-线程池配置"><a href="#1-2-SpringBoot-线程池配置" class="headerlink" title="1.2 SpringBoot 线程池配置"></a>1.2 SpringBoot 线程池配置</h3><p>基于 <code>ThreadPoolTaskExecutor</code> 封装，替代默认线程池，生产环境推荐配置：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.EnableAsync;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ThreadPoolExecutor;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAsync</span> <span class="comment">// 开启异步支持</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadPoolConfig</span> &#123;</span><br><span class="line">    <span class="meta">@Bean(&quot;customThreadPool&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> ThreadPoolTaskExecutor <span class="title function_">customThreadPool</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">ThreadPoolTaskExecutor</span> <span class="variable">executor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolTaskExecutor</span>();</span><br><span class="line">        executor.setCorePoolSize(<span class="number">5</span>);        <span class="comment">// 核心线程数</span></span><br><span class="line">        executor.setMaxPoolSize(<span class="number">10</span>);        <span class="comment">// 最大线程数</span></span><br><span class="line">        executor.setQueueCapacity(<span class="number">20</span>);      <span class="comment">// 队列容量</span></span><br><span class="line">        executor.setKeepAliveSeconds(<span class="number">60</span>);   <span class="comment">// 非核心线程空闲时间</span></span><br><span class="line">        executor.setThreadNamePrefix(<span class="string">&quot;custom-thread-&quot;</span>); <span class="comment">// 线程名前缀（便于排查）</span></span><br><span class="line">        <span class="comment">// 拒绝策略：提交线程自己执行，避免任务丢失</span></span><br><span class="line">        executor.setRejectedExecutionHandler(<span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.CallerRunsPolicy());</span><br><span class="line">        executor.initialize();</span><br><span class="line">        <span class="keyword">return</span> executor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="1-3-线程池基础使用（无回调异步任务）"><a href="#1-3-线程池基础使用（无回调异步任务）" class="headerlink" title="1.3 线程池基础使用（无回调异步任务）"></a>1.3 线程池基础使用（无回调异步任务）</h3><p>无回调异步任务：提交后主线程不等待、不获取结果，适用于日志、通知等无需结果的场景。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.Async;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncTaskService</span> &#123;</span><br><span class="line">    <span class="comment">// 绑定自定义线程池，执行无回调异步任务</span></span><br><span class="line">    <span class="meta">@Async(&quot;customThreadPool&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">noCallbackTask</span><span class="params">(String taskName)</span> &#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">&quot; 执行无回调任务：&quot;</span> + taskName);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>); <span class="comment">// 模拟业务耗时</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="二、异步回调（无参回调-vs-有参回调）"><a href="#二、异步回调（无参回调-vs-有参回调）" class="headerlink" title="二、异步回调（无参回调 vs 有参回调）"></a>二、异步回调（无参回调 vs 有参回调）</h2><p>异步回调解决「无回调任务无法获取结果、处理异常」的问题，分为<strong>无参回调</strong>（仅通知完成，无参数）和<strong>有参回调</strong>（接收任务结果&#x2F;异常，核心场景），基于 <code>CompletableFuture</code> 实现。</p><h3 id="2-1-核心概念"><a href="#2-1-核心概念" class="headerlink" title="2.1 核心概念"></a>2.1 核心概念</h3><ul><li>无参回调：任务完成后触发回调，回调函数无参数，仅做简单通知；</li><li>有参回调：任务完成后触发回调，回调函数接收<strong>任务结果</strong>（成功）或<strong>异常信息</strong>（失败），支持结果处理、链式逻辑。</li></ul><h3 id="2-2-无参回调实现"><a href="#2-2-无参回调实现" class="headerlink" title="2.2 无参回调实现"></a>2.2 无参回调实现</h3><p>适用于仅需通知任务完成，无需处理结果的场景：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Executor;</span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CallbackService</span> &#123;</span><br><span class="line">    <span class="meta">@Resource(name = &quot;customThreadPool&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Executor threadPool;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 无参回调：任务完成后仅通知，无参数</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">noParamCallback</span><span class="params">()</span> &#123;</span><br><span class="line">        CompletableFuture.runAsync(() -&gt; &#123;</span><br><span class="line">            <span class="comment">// 异步任务逻辑</span></span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">&quot; 执行异步任务&quot;</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123; Thread.sleep(<span class="number">1000</span>); &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;&#125;</span><br><span class="line">        &#125;, threadPool)</span><br><span class="line">        <span class="comment">// 无参成功回调：无参数，仅打印完成通知</span></span><br><span class="line">        .thenRun(() -&gt; System.out.println(<span class="string">&quot;无参回调：任务执行完成&quot;</span>))</span><br><span class="line">        <span class="comment">// 无参失败回调：无参数，仅打印异常通知</span></span><br><span class="line">        .exceptionally(ex -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;无参回调：任务执行异常&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-3-有参回调实现（核心）"><a href="#2-3-有参回调实现（核心）" class="headerlink" title="2.3 有参回调实现（核心）"></a>2.3 有参回调实现（核心）</h3><p>适用于需要处理任务结果、异常的场景，是生产环境主流用法，结合 <code>@Async</code> 整合Spring生态：</p><h4 id="步骤1：异步任务返回-CompletableFuture"><a href="#步骤1：异步任务返回-CompletableFuture" class="headerlink" title="步骤1：异步任务返回 CompletableFuture"></a>步骤1：异步任务返回 <code>CompletableFuture</code></h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncCallbackService</span> &#123;</span><br><span class="line">    <span class="comment">// 有参异步任务：返回CompletableFuture，携带结果/异常</span></span><br><span class="line">    <span class="meta">@Async(&quot;customThreadPool&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> CompletableFuture&lt;String&gt; <span class="title function_">withParamTask</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            <span class="comment">// 模拟业务成功，返回结果</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> <span class="string">&quot;用户【&quot;</span> + username + <span class="string">&quot;】注册成功，ID：&quot;</span> + System.currentTimeMillis();</span><br><span class="line">            <span class="keyword">return</span> CompletableFuture.completedFuture(result);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// 模拟业务异常，返回异常信息</span></span><br><span class="line">            <span class="keyword">return</span> CompletableFuture.failedFuture(<span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">&quot;注册失败：&quot;</span> + e.getMessage()));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="步骤2：绑定有参回调（接收结果-x2F-异常）"><a href="#步骤2：绑定有参回调（接收结果-x2F-异常）" class="headerlink" title="步骤2：绑定有参回调（接收结果&#x2F;异常）"></a>步骤2：绑定有参回调（接收结果&#x2F;异常）</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CallbackController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> AsyncCallbackService asyncCallbackService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/register&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">register</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用异步任务，获取CompletableFuture</span></span><br><span class="line">        CompletableFuture&lt;String&gt; future = asyncCallbackService.withParamTask(username);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 有参成功回调：接收任务结果，处理业务逻辑</span></span><br><span class="line">        future.thenAccept(result -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;有参回调（成功）：&quot;</span> + result);</span><br><span class="line">            <span class="comment">// 后续逻辑：如发送短信、更新数据库</span></span><br><span class="line">        &#125;)</span><br><span class="line">        <span class="comment">// 有参失败回调：接收异常信息，做兜底处理</span></span><br><span class="line">        .exceptionally(ex -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;有参回调（失败）：&quot;</span> + ex.getMessage());</span><br><span class="line">            <span class="comment">// 后续逻辑：如记录日志、发送告警</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 主线程直接返回，无需等待</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;注册请求已提交，后台异步处理中...&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-4-进阶：链式有参回调"><a href="#2-4-进阶：链式有参回调" class="headerlink" title="2.4 进阶：链式有参回调"></a>2.4 进阶：链式有参回调</h3><p>多个异步任务依赖前序结果时，用链式回调实现线性逻辑（类JS async&#x2F;await 思维）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 链式有参回调：注册→发送短信→更新状态</span></span><br><span class="line">CompletableFuture.supplyAsync(() -&gt; <span class="string">&quot;用户ID：1001&quot;</span>, threadPool)</span><br><span class="line">    .thenApply(userId -&gt; &#123;</span><br><span class="line">        <span class="comment">// 基于前序结果，执行第二个异步任务</span></span><br><span class="line">        System.out.println(<span class="string">&quot;为&quot;</span> + userId + <span class="string">&quot;发送注册短信&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> userId + <span class="string">&quot;：短信发送成功&quot;</span>;</span><br><span class="line">    &#125;)</span><br><span class="line">    .thenAccept(smsResult -&gt; &#123;</span><br><span class="line">        <span class="comment">// 基于短信结果，执行最终逻辑</span></span><br><span class="line">        System.out.println(smsResult + <span class="string">&quot;，更新用户状态为已激活&quot;</span>);</span><br><span class="line">    &#125;)</span><br><span class="line">    .exceptionally(ex -&gt; &#123;</span><br><span class="line">        <span class="comment">// 全局异常捕获：任意一步失败均触发</span></span><br><span class="line">        System.out.println(<span class="string">&quot;流程异常：&quot;</span> + ex.getMessage());</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure><hr><h2 id="三、Java同步关键字：synchronized-amp-volatile"><a href="#三、Java同步关键字：synchronized-amp-volatile" class="headerlink" title="三、Java同步关键字：synchronized &amp; volatile"></a>三、Java同步关键字：synchronized &amp; volatile</h2><p>解决多线程下共享变量的<strong>可见性、原子性、有序性</strong>问题，是并发编程的基础，日常开发虽被框架封装，但底层原理必须掌握。</p><h3 id="3-1-volatile-关键字（轻量级无锁）"><a href="#3-1-volatile-关键字（轻量级无锁）" class="headerlink" title="3.1 volatile 关键字（轻量级无锁）"></a>3.1 volatile 关键字（轻量级无锁）</h3><h4 id="核心特性"><a href="#核心特性" class="headerlink" title="核心特性"></a>核心特性</h4><ul><li>保证<strong>可见性</strong>：一个线程修改共享变量，其他线程立即看到最新值（不读线程缓存）；</li><li>禁止<strong>指令重排</strong>：JVM不打乱代码执行顺序，避免多线程逻辑混乱；</li><li><strong>不保证原子性</strong>：复合操作（如<code>i++</code>）用volatile仍会出错，仅适用于「一写多读」场景。</li></ul><h4 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h4><p>线程状态标记、单例双重检查、开关控制等「一个线程写，多个线程读」的场景。</p><h4 id="示例：volatile-控制线程停止"><a href="#示例：volatile-控制线程停止" class="headerlink" title="示例：volatile 控制线程停止"></a>示例：volatile 控制线程停止</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">VolatileDemo</span> &#123;</span><br><span class="line">    <span class="comment">// 加volatile：保证可见性，主线程修改后子线程立即感知</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">isRunning</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 子线程：循环执行，直到isRunning为false</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">while</span> (isRunning) &#123;&#125;</span><br><span class="line">            System.out.println(<span class="string">&quot;子线程停止成功&quot;</span>);</span><br><span class="line">        &#125;).start();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 主线程：1秒后修改状态</span></span><br><span class="line">        Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        isRunning = <span class="literal">false</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;主线程已修改状态&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-2-synchronized-关键字（重量级内置锁）"><a href="#3-2-synchronized-关键字（重量级内置锁）" class="headerlink" title="3.2 synchronized 关键字（重量级内置锁）"></a>3.2 synchronized 关键字（重量级内置锁）</h3><h4 id="核心特性-1"><a href="#核心特性-1" class="headerlink" title="核心特性"></a>核心特性</h4><ul><li>保证<strong>原子性</strong>：加锁代码同一时间仅一个线程执行，复合操作不被打断；</li><li>保证<strong>可见性</strong>：释放锁时刷回主内存，加锁时读取最新值；</li><li>保证<strong>有序性</strong>：加锁代码按顺序执行，禁止指令重排；</li><li>缺点：加锁&#x2F;解锁有开销，竞争线程会阻塞（悲观锁）。</li></ul><h4 id="适用场景-1"><a href="#适用场景-1" class="headerlink" title="适用场景"></a>适用场景</h4><p>多线程修改共享资源、计数器、库存扣减、共享集合操作等「多线程竞争写」场景。</p><h4 id="示例：synchronized-保证计数器安全"><a href="#示例：synchronized-保证计数器安全" class="headerlink" title="示例：synchronized 保证计数器安全"></a>示例：synchronized 保证计数器安全</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SynchronizedDemo</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="variable">count</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 加synchronized：保证increment方法原子性，多线程串行执行</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">increment</span><span class="params">()</span> &#123;</span><br><span class="line">        count++; <span class="comment">// 复合操作：读→改→写</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        Thread[] threads = <span class="keyword">new</span> <span class="title class_">Thread</span>[<span class="number">10</span>];</span><br><span class="line">        <span class="comment">// 10个线程，每个线程执行1000次计数</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            threads[i] = <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; <span class="number">1000</span>; j++) &#123;</span><br><span class="line">                    increment();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            threads[i].start();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 等待所有线程执行完毕</span></span><br><span class="line">        <span class="keyword">for</span> (Thread thread : threads) &#123;</span><br><span class="line">            thread.join();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 结果永远为10000，保证线程安全</span></span><br><span class="line">        System.out.println(<span class="string">&quot;最终计数：&quot;</span> + count);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-3-核心区别对比"><a href="#3-3-核心区别对比" class="headerlink" title="3.3 核心区别对比"></a>3.3 核心区别对比</h3><table><thead><tr><th>对比维度</th><th>volatile</th><th>synchronized</th></tr></thead><tbody><tr><td>核心作用</td><td>可见性、禁止重排，不保证原子性</td><td>原子性、可见性、有序性（全特性）</td></tr><tr><td>实现原理</td><td>无锁，依赖内存屏障</td><td>加对象锁&#x2F;类锁，阻塞竞争线程</td></tr><tr><td>性能开销</td><td>极轻量，几乎无开销</td><td>较重，竞争时会阻塞</td></tr><tr><td>适用范围</td><td>仅修饰共享变量</td><td>修饰实例方法、静态方法、代码块</td></tr><tr><td>典型场景</td><td>一写多读、状态标记</td><td>多线程写共享资源、复合操作</td></tr></tbody></table><hr><h2 id="四、综合实战：线程池-有参回调-同步关键字"><a href="#四、综合实战：线程池-有参回调-同步关键字" class="headerlink" title="四、综合实战：线程池 + 有参回调 + 同步关键字"></a>四、综合实战：线程池 + 有参回调 + 同步关键字</h2><p>整合所有知识点，实现「自定义线程池执行异步任务，用volatile控制状态，synchronized保证计数器安全，有参回调处理结果」的完整场景：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Executor;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntegratedService</span> &#123;</span><br><span class="line">    <span class="meta">@Resource(name = &quot;customThreadPool&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Executor threadPool;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 共享计数器：用synchronized保证线程安全</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">int</span> <span class="variable">taskCount</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 线程状态标记：用volatile保证可见性</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">isTaskEnabled</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 同步方法：原子性计数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">addTaskCount</span><span class="params">()</span> &#123;</span><br><span class="line">        taskCount++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 整合异步任务：线程池执行 + 有参回调 + 同步关键字</span></span><br><span class="line">    <span class="keyword">public</span> CompletableFuture&lt;String&gt; <span class="title function_">integratedTask</span><span class="params">(String taskName)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> CompletableFuture.supplyAsync(() -&gt; &#123;</span><br><span class="line">            <span class="comment">// 1. 用volatile判断状态，控制任务执行</span></span><br><span class="line">            <span class="keyword">if</span> (!isTaskEnabled) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(<span class="string">&quot;任务已禁用，无法执行&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 2. 用synchronized保证计数安全</span></span><br><span class="line">            addTaskCount();</span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">&quot; 执行任务：&quot;</span> + taskName + <span class="string">&quot;，当前总任务数：&quot;</span> + taskCount);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 3. 模拟业务耗时</span></span><br><span class="line">            <span class="keyword">try</span> &#123; Thread.sleep(<span class="number">1000</span>); &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;&#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;任务【&quot;</span> + taskName + <span class="string">&quot;】执行完成，总任务数：&quot;</span> + taskCount;</span><br><span class="line">        &#125;, threadPool);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 控制任务状态：volatile修改后立即生效</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setTaskEnabled</span><span class="params">(<span class="type">boolean</span> enabled)</span> &#123;</span><br><span class="line">        isTaskEnabled = enabled;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>调用示例</strong>：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">IntegratedController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> IntegratedService integratedService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/integratedTask&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">doTask</span><span class="params">(String taskName)</span> &#123;</span><br><span class="line">        CompletableFuture&lt;String&gt; future = integratedService.integratedTask(taskName);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 有参回调处理结果</span></span><br><span class="line">        future.thenAccept(result -&gt; System.out.println(<span class="string">&quot;有参回调：&quot;</span> + result))</span><br><span class="line">              .exceptionally(ex -&gt; &#123;</span><br><span class="line">                  System.out.println(<span class="string">&quot;有参回调（异常）：&quot;</span> + ex.getMessage());</span><br><span class="line">                  <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">              &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;任务已提交，后台异步执行中...&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/disableTask&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">disableTask</span><span class="params">()</span> &#123;</span><br><span class="line">        integratedService.setTaskEnabled(<span class="literal">false</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;任务已禁用&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="五、核心要点总结"><a href="#五、核心要点总结" class="headerlink" title="五、核心要点总结"></a>五、核心要点总结</h2><ol><li><strong>线程池</strong>：核心遵循「核心线程→队列→最大线程→拒绝策略」，SpringBoot用<code>ThreadPoolTaskExecutor</code>自定义，兼顾性能与稳定性；</li><li><strong>异步回调</strong>：无参回调仅做完成通知，有参回调接收结果&#x2F;异常（核心），基于<code>CompletableFuture</code>实现，支持链式逻辑；</li><li><strong>volatile</strong>：轻量无锁，管「可见性+禁止重排」，适用于一写多读的状态标记，别用于多线程写；</li><li><strong>synchronized</strong>：重量加锁，管「原子性+可见性+有序性」，适用于多线程竞争写共享资源，保证线程安全；</li><li><strong>整合使用</strong>：线程池负责并发执行，回调处理结果，同步关键字解决共享变量安全问题，三者配合实现高效、安全的并发编程。</li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>数据库字段命名规范及核心原因</title>
      <link href="/2026/02/04/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AD%97%E6%AE%B5%E5%91%BD%E5%90%8D%E8%A7%84%E8%8C%83%E5%8F%8A%E6%A0%B8%E5%BF%83%E5%8E%9F%E5%9B%A0/"/>
      <url>/2026/02/04/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%AD%97%E6%AE%B5%E5%91%BD%E5%90%8D%E8%A7%84%E8%8C%83%E5%8F%8A%E6%A0%B8%E5%BF%83%E5%8E%9F%E5%9B%A0/</url>
      
        <content type="html"><![CDATA[<h1 id="数据库字段命名规范及核心原因"><a href="#数据库字段命名规范及核心原因" class="headerlink" title="数据库字段命名规范及核心原因"></a>数据库字段命名规范及核心原因</h1><p>结合行业通用标准、SQL 语言特性及企业级开发实践，整理数据库字段命名的核心规范、背后原因及示例，同时适配 SpringBoot + MyBatis-Plus 等主流技术栈，兼顾规范性、兼容性与可维护性。</p><h2 id="一、核心命名规范（行业通用标准）"><a href="#一、核心命名规范（行业通用标准）" class="headerlink" title="一、核心命名规范（行业通用标准）"></a>一、核心命名规范（行业通用标准）</h2><h3 id="1-基础格式规范"><a href="#1-基础格式规范" class="headerlink" title="1. 基础格式规范"></a>1. 基础格式规范</h3><table><thead><tr><th>规范项</th><th>要求</th><th>示例</th></tr></thead><tbody><tr><td>分隔符</td><td>统一使用<strong>下划线（蛇形命名法）</strong> 分隔单词，禁止使用驼峰、空格、特殊符号</td><td><code>user_name</code>、<code>order_pay_time</code></td></tr><tr><td>大小写</td><td>全小写字母，禁止大小写混用、全大写</td><td>正确：<code>created_at</code>；错误：<code>CreatedAt</code>、<code>CREATED_AT</code></td></tr><tr><td>语义性</td><td>必须见名知意，使用<strong>标准英文单词</strong>，禁止拼音、无意义缩写（通用行业缩写除外）</td><td>正确：<code>user_phone</code>；错误：<code>yh_sj</code>（用户手机拼音）、<code>usr_ph</code>（非通用缩写）</td></tr><tr><td>简洁性</td><td>长度控制在 30 字符内，避免冗余描述，核心信息前置</td><td>正确：<code>order_status</code>；错误：<code>order_current_status_info</code></td></tr><tr><td>禁止保留字</td><td>避免使用 SQL 关键字（如 <code>order</code>、<code>user</code>、<code>status</code> 可加前缀，或用同义替换）</td><td>正确：<code>order_info</code>、<code>user_account</code>；错误：直接用 <code>order</code> 作为字段名</td></tr></tbody></table><h3 id="2-时态与功能规范"><a href="#2-时态与功能规范" class="headerlink" title="2. 时态与功能规范"></a>2. 时态与功能规范</h3><h4 id="（1）操作类字段（创建-x2F-更新-x2F-删除）"><a href="#（1）操作类字段（创建-x2F-更新-x2F-删除）" class="headerlink" title="（1）操作类字段（创建&#x2F;更新&#x2F;删除）"></a>（1）操作类字段（创建&#x2F;更新&#x2F;删除）</h4><ul><li>表示“已完成的操作”，统一使用<strong>过去分词</strong>（符合英语语法，且与行业惯例一致）</li><li>时间字段后缀统一用 <code>_at</code>，操作人字段后缀统一用 <code>_by</code>&#x2F;<code>_id</code><table><thead><tr><th>字段类型</th><th>规范命名</th><th>示例</th></tr></thead><tbody><tr><td>创建时间</td><td><code>xxx_at</code></td><td><code>created_at</code>、<code>order_created_at</code></td></tr><tr><td>创建人</td><td><code>xxx_by</code>&#x2F;<code>xxx_user_id</code></td><td><code>created_by</code>、<code>created_user_id</code></td></tr><tr><td>更新时间</td><td><code>xxx_at</code></td><td><code>updated_at</code></td></tr><tr><td>更新人</td><td><code>xxx_by</code>&#x2F;<code>xxx_user_id</code></td><td><code>updated_by</code></td></tr><tr><td>删除时间（软删除）</td><td><code>xxx_at</code></td><td><code>deleted_at</code></td></tr><tr><td>删除人（软删除）</td><td><code>xxx_by</code>&#x2F;<code>xxx_user_id</code></td><td><code>deleted_by</code></td></tr></tbody></table></li></ul><h4 id="（2）状态-x2F-布尔类字段"><a href="#（2）状态-x2F-布尔类字段" class="headerlink" title="（2）状态&#x2F;布尔类字段"></a>（2）状态&#x2F;布尔类字段</h4><ul><li>布尔值（是&#x2F;否）：统一前缀 <code>is_</code>，值为 0&#x2F;1 或 <code>true</code>&#x2F;<code>false</code></li><li>枚举状态：后缀 <code>_status</code>&#x2F;<code>_type</code>，明确字段含义<table><thead><tr><th>字段类型</th><th>规范命名</th><th>示例</th></tr></thead><tbody><tr><td>布尔状态</td><td><code>is_xxx</code></td><td><code>is_deleted</code>（是否删除）、<code>is_enable</code>（是否启用）</td></tr><tr><td>业务状态</td><td><code>xxx_status</code></td><td><code>order_status</code>（订单状态）、<code>user_status</code>（用户状态）</td></tr><tr><td>类型区分</td><td><code>xxx_type</code></td><td><code>pay_type</code>（支付类型）、<code>user_type</code>（用户类型）</td></tr></tbody></table></li></ul><h4 id="（3）主键与外键规范"><a href="#（3）主键与外键规范" class="headerlink" title="（3）主键与外键规范"></a>（3）主键与外键规范</h4><ul><li>主键：单表主键统一命名为 <code>id</code>（自增&#x2F;UUID 均可），无需加表名前缀</li><li>外键：关联表主键 + <code>_id</code>，严格与关联表主键命名一致<table><thead><tr><th>字段类型</th><th>规范命名</th><th>示例</th></tr></thead><tbody><tr><td>单表主键</td><td><code>id</code></td><td>用户表主键 <code>id</code>、订单表主键 <code>id</code></td></tr><tr><td>关联外键</td><td><code>关联表名_id</code></td><td>订单表关联用户表：<code>user_id</code>；订单详情表关联订单表：<code>order_id</code></td></tr></tbody></table></li></ul><h3 id="3-通用审计字段规范（所有业务表标配）"><a href="#3-通用审计字段规范（所有业务表标配）" class="headerlink" title="3. 通用审计字段规范（所有业务表标配）"></a>3. 通用审计字段规范（所有业务表标配）</h3><p>成熟系统的业务表必须包含以下审计字段，命名统一，无特殊情况不修改：</p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">id              <span class="type">BIGINT</span>      <span class="keyword">NOT</span> <span class="keyword">NULL</span> AUTO_INCREMENT COMMENT <span class="string">&#x27;主键ID&#x27;</span>,</span><br><span class="line"><span class="comment">-- 核心业务字段 ...</span></span><br><span class="line">created_by      <span class="type">BIGINT</span>      COMMENT <span class="string">&#x27;创建人ID&#x27;</span>,</span><br><span class="line">created_at      DATETIME    <span class="keyword">NOT</span> <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> COMMENT <span class="string">&#x27;创建时间&#x27;</span>,</span><br><span class="line">updated_by      <span class="type">BIGINT</span>      COMMENT <span class="string">&#x27;更新人ID&#x27;</span>,</span><br><span class="line">updated_at      DATETIME    <span class="keyword">NOT</span> <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="built_in">CURRENT_TIMESTAMP</span> COMMENT <span class="string">&#x27;更新时间&#x27;</span>,</span><br><span class="line">is_deleted      TINYINT     <span class="keyword">NOT</span> <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="number">0</span> COMMENT <span class="string">&#x27;是否删除：0-未删除，1-已删除&#x27;</span>,</span><br><span class="line"><span class="keyword">PRIMARY</span> KEY (`id`)</span><br></pre></td></tr></table></figure><h3 id="4-特殊场景补充规范"><a href="#4-特殊场景补充规范" class="headerlink" title="4. 特殊场景补充规范"></a>4. 特殊场景补充规范</h3><ol><li><strong>通用缩写</strong>：仅允许使用行业通用缩写（如 <code>id</code>、<code>no</code>、<code>code</code>、<code>amt</code>、<code>qty</code>），禁止自定义缩写<br>示例：<code>order_no</code>（订单号）、<code>pay_amt</code>（支付金额）、<code>stock_qty</code>（库存数量）</li><li><strong>多表关联字段</strong>：跨表关联的相同含义字段，命名必须完全一致<br>示例：用户表 <code>id</code>，所有关联表统一用 <code>user_id</code>，禁止出现 <code>uid</code>、<code>userid</code> 等变体</li><li><strong>时间字段</strong>：精确到时分秒用 <code>DATETIME</code>，仅日期用 <code>date</code>，时间字段后缀统一 <code>_at</code><br>示例：<code>pay_time</code> 改为 <code>pay_at</code>，<code>create_time</code> 改为 <code>created_at</code></li></ol><h2 id="二、规范背后的核心原因"><a href="#二、规范背后的核心原因" class="headerlink" title="二、规范背后的核心原因"></a>二、规范背后的核心原因</h2><h3 id="1-适配-SQL-语言的底层特性（最关键原因）"><a href="#1-适配-SQL-语言的底层特性（最关键原因）" class="headerlink" title="1. 适配 SQL 语言的底层特性（最关键原因）"></a>1. 适配 SQL 语言的底层特性（最关键原因）</h3><p>SQL 标准规定<strong>标识符（表名、字段名）默认不区分大小写</strong>，不同数据库实现存在差异：</p><ul><li>MySQL Windows 不区分大小写，Linux 默认区分；PostgreSQL&#x2F;Oracle 会将未加引号的标识符转为小写；</li><li>若使用驼峰命名（如 <code>userName</code>），数据库会默认解析为 <code>username</code>，语义丢失；若保留驼峰，必须用引号&#x2F;反引号包裹（如 <code>userName</code>），书写繁琐且易报错；</li><li>下划线命名（<code>user_name</code>）天然分隔单词，无需额外符号，完美适配 SQL 大小写特性，是最实用的选择。</li></ul><h3 id="2-提升跨系统-x2F-工具的兼容性"><a href="#2-提升跨系统-x2F-工具的兼容性" class="headerlink" title="2. 提升跨系统&#x2F;工具的兼容性"></a>2. 提升跨系统&#x2F;工具的兼容性</h3><ul><li>主流数据库（MySQL、PostgreSQL、Oracle、SQL Server）、数据工具（Navicat、DBeaver）、数据同步工具（Canal、DataX）均原生支持下划线命名，无兼容性问题；</li><li>跨语言开发（Java、Python、Go、PHP）时，下划线命名是全语言通用风格，无需额外做字段名映射；</li><li>驼峰命名在老旧工具、跨库迁移场景中易出现解析错误，兼容性远低于下划线。</li></ul><h3 id="3-增强可读性与团队协作效率"><a href="#3-增强可读性与团队协作效率" class="headerlink" title="3. 增强可读性与团队协作效率"></a>3. 增强可读性与团队协作效率</h3><ul><li>下划线分隔的单词视觉上更易区分，长字段名（如 <code>user_login_failed_count</code>、<code>order_pay_expire_at</code>）比驼峰更易快速阅读；</li><li>统一的命名规范让团队成员无需猜测字段含义，降低沟通成本，新人接手项目可快速理解表结构；</li><li>时态规范（<code>created</code> 而非 <code>create</code>）符合英语语法，更贴合“已完成操作”的语义，避免歧义。</li></ul><h3 id="4-适配主流框架的生态特性"><a href="#4-适配主流框架的生态特性" class="headerlink" title="4. 适配主流框架的生态特性"></a>4. 适配主流框架的生态特性</h3><p>SpringBoot + MyBatis-Plus、JPA、Hibernate 等主流 ORM 框架<strong>原生支持下划线与 Java 驼峰的自动映射</strong>：</p><ul><li>数据库 <code>user_name</code> 自动映射为 Java 实体类 <code>userName</code>，无需手动配置 <code>@Column</code> 注解；</li><li>若数据库用驼峰，需手动配置映射关系，增加开发成本，违背框架的“约定大于配置”理念。</li></ul><h3 id="5-保证可维护性与扩展性"><a href="#5-保证可维护性与扩展性" class="headerlink" title="5. 保证可维护性与扩展性"></a>5. 保证可维护性与扩展性</h3><ul><li>统一的命名规则让表结构更规整，后期新增字段、修改字段时，可快速遵循规范，避免字段命名混乱；</li><li>审计字段、外键、状态字段的统一命名，便于后续做数据统计、权限控制、日志审计等扩展功能；</li><li>禁止拼音、无意义缩写，避免后期维护时无法理解字段含义，减少技术债务。</li></ul><h2 id="三、规范示例-vs-反例对比"><a href="#三、规范示例-vs-反例对比" class="headerlink" title="三、规范示例 vs 反例对比"></a>三、规范示例 vs 反例对比</h2><table><thead><tr><th>字段含义</th><th>规范示例</th><th>反例（不推荐）</th><th>反例问题</th></tr></thead><tbody><tr><td>用户姓名</td><td><code>user_name</code></td><td><code>userName</code>、<code>UserName</code>、<code>yhxm</code></td><td>驼峰命名、拼音、大小写混用</td></tr><tr><td>创建时间</td><td><code>created_at</code></td><td><code>create_time</code>、<code>CreateAt</code>、<code>c_time</code></td><td>时态错误、缩写无意义、驼峰</td></tr><tr><td>订单状态</td><td><code>order_status</code></td><td><code>orderState</code>、<code>ddzt</code>、<code>status</code></td><td>驼峰、拼音、使用 SQL 保留字</td></tr><tr><td>是否启用</td><td><code>is_enable</code></td><td><code>enable</code>、<code>isEnable</code>、<code>sfqy</code></td><td>缺少前缀、驼峰、拼音</td></tr><tr><td>关联用户ID</td><td><code>user_id</code></td><td><code>uid</code>、<code>userId</code>、<code>userid</code></td><td>非标准缩写、驼峰、命名不统一</td></tr><tr><td>支付完成时间</td><td><code>pay_finish_at</code></td><td><code>payFinishTime</code>、<code>pay_time</code></td><td>驼峰、后缀不规范、语义模糊</td></tr></tbody></table><h2 id="四、最佳实践与注意事项"><a href="#四、最佳实践与注意事项" class="headerlink" title="四、最佳实践与注意事项"></a>四、最佳实践与注意事项</h2><ol><li><strong>团队规范统一</strong>：项目初期制定命名规范文档，所有成员严格遵守，禁止同一项目中出现多种命名风格（如混用 <code>created_by</code> 和 <code>create_user_id</code>）；</li><li><strong>避免过度冗余</strong>：无需为字段加无意义前缀&#x2F;后缀，如 <code>user_info_name</code>（冗余 <code>info</code>），直接用 <code>user_name</code> 即可；</li><li><strong>软删除优先</strong>：用 <code>is_deleted</code> 实现软删除，而非物理删除，配合 <code>deleted_at</code>&#x2F;<code>deleted_by</code> 记录删除信息；</li><li><strong>字段注释必填</strong>：每个字段必须加 <code>COMMENT</code> 注释，明确字段含义、枚举值含义，提升可维护性；</li><li><strong>适配业务场景</strong>：通用规范为基础，可根据业务做少量调整（如金融系统金额字段统一加 <code>_amt</code>），但核心规则不变。</li></ol><h2 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a>五、总结</h2><p>数据库字段命名规范的核心是<strong>“适配 SQL 特性、兼顾兼容性、提升可读性”</strong>，下划线+全小写+语义化+统一时态是行业通用的最优解。遵循规范不仅能避免技术兼容性问题，还能大幅提升团队协作效率和项目可维护性，是企业级数据库设计的基础要求。对于 SpringBoot + MyBatis-Plus 项目，下划线命名还能享受框架自动映射的便利，是性价比极高的规范实践。</p>]]></content>
      
      
      
        <tags>
            
            <tag> db </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Spring 的事务管理机制输出的警告日志</title>
      <link href="/2026/02/04/Spring-%E7%9A%84%E4%BA%8B%E5%8A%A1%E7%AE%A1%E7%90%86%E6%9C%BA%E5%88%B6%E8%BE%93%E5%87%BA%E7%9A%84%E8%AD%A6%E5%91%8A%E6%97%A5%E5%BF%97/"/>
      <url>/2026/02/04/Spring-%E7%9A%84%E4%BA%8B%E5%8A%A1%E7%AE%A1%E7%90%86%E6%9C%BA%E5%88%B6%E8%BE%93%E5%87%BA%E7%9A%84%E8%AD%A6%E5%91%8A%E6%97%A5%E5%BF%97/</url>
      
        <content type="html"><![CDATA[<h2 id="终端提示was-not-registered-for-synchronization-because-synchronization-is-not-active-JDBC-Connection-com-mysql-cj-jdbc-ConnectionImpl-5094b8fe-will-not-be-managed-by-Spring"><a href="#终端提示was-not-registered-for-synchronization-because-synchronization-is-not-active-JDBC-Connection-com-mysql-cj-jdbc-ConnectionImpl-5094b8fe-will-not-be-managed-by-Spring" class="headerlink" title="终端提示was not registered for synchronization because synchronization is not active JDBC Connection [com.mysql.cj.jdbc.ConnectionImpl@5094b8fe] will not be managed by Spring"></a>终端提示was not registered for synchronization because synchronization is not active JDBC Connection [com.mysql.cj.jdbc.ConnectionImpl@5094b8fe] will not be managed by Spring</h2><h1 id="SpringBoot-事务警告日志详解及-MyBatis-Plus-ORM-使用规范"><a href="#SpringBoot-事务警告日志详解及-MyBatis-Plus-ORM-使用规范" class="headerlink" title="SpringBoot 事务警告日志详解及 MyBatis-Plus ORM 使用规范"></a>SpringBoot 事务警告日志详解及 MyBatis-Plus ORM 使用规范</h1><h2 id="一、问题概述"><a href="#一、问题概述" class="headerlink" title="一、问题概述"></a>一、问题概述</h2><p>在 SpringBoot + MyBatis-Plus 项目开发中，终端常出现以下警告日志，并非错误，不影响功能正常运行，但需明确其产生原因及合理处理方式：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">was not registered for synchronization because synchronization is not active</span><br><span class="line">JDBC Connection [com.mysql.cj.jdbc.ConnectionImpl@5094b8fe] will not be managed by Spring</span><br></pre></td></tr></table></figure><h2 id="二、警告原因深度分析"><a href="#二、警告原因深度分析" class="headerlink" title="二、警告原因深度分析"></a>二、警告原因深度分析</h2><h3 id="1-日志核心含义"><a href="#1-日志核心含义" class="headerlink" title="1. 日志核心含义"></a>1. 日志核心含义</h3><p>两句日志本质是 Spring 事务管理机制的<strong>状态提示</strong>，告知开发者当前 JDBC 操作未处于事务上下文，Spring 不会对该数据库连接进行事务管控：</p><ul><li><code>was not registered for synchronization because synchronization is not active</code>：Spring 事务同步机制未激活，无法将当前 JDBC 操作纳入事务同步流程；</li><li><code>JDBC Connection ... will not be managed by Spring</code>：当前数据库连接不会被 Spring 事务管理器管理（不参与事务提交&#x2F;回滚、不绑定线程事务上下文）。</li></ul><h3 id="2-根本原因"><a href="#2-根本原因" class="headerlink" title="2. 根本原因"></a>2. 根本原因</h3><p>执行数据库操作时<strong>未激活 Spring 事务</strong>（方法&#x2F;类未添加 <code>@Transactional</code> 注解）。Spring 事务管理仅对标记 <code>@Transactional</code> 的逻辑生效，普通无事务的查询操作，Spring 不会介入连接管理，因此输出该提示。</p><blockquote><p>关键结论：该警告与「未加 <code>@Transactional</code>」直接相关，但<strong>普通查询场景不加事务是合理设计</strong>，警告本身无害。</p></blockquote><h2 id="三、解决方案"><a href="#三、解决方案" class="headerlink" title="三、解决方案"></a>三、解决方案</h2><h3 id="方案1：直接忽略警告（推荐，99-场景适用）"><a href="#方案1：直接忽略警告（推荐，99-场景适用）" class="headerlink" title="方案1：直接忽略警告（推荐，99%场景适用）"></a>方案1：直接忽略警告（推荐，99%场景适用）</h3><p>普通查询接口无需事务支持，该警告不影响功能、性能及稳定性，是 Spring 的正常日志输出，直接忽略即可，无需任何代码修改。</p><h3 id="方案2：关闭警告日志（可选，仅为日志整洁）"><a href="#方案2：关闭警告日志（可选，仅为日志整洁）" class="headerlink" title="方案2：关闭警告日志（可选，仅为日志整洁）"></a>方案2：关闭警告日志（可选，仅为日志整洁）</h3><p>若觉得日志干扰，可通过调整日志级别关闭该提示，<strong>无需为消除警告强行添加事务</strong>，配置如下：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># application.yml 配置</span></span><br><span class="line"><span class="attr">logging:</span></span><br><span class="line">  <span class="attr">level:</span></span><br><span class="line">    <span class="attr">org.springframework.jdbc.datasource.DataSourceUtils:</span> <span class="string">OFF</span> <span class="comment"># 关闭对应警告日志</span></span><br></pre></td></tr></table></figure><h3 id="方案3：按需添加事务（仅特殊场景）"><a href="#方案3：按需添加事务（仅特殊场景）" class="headerlink" title="方案3：按需添加事务（仅特殊场景）"></a>方案3：按需添加事务（仅特殊场景）</h3><p>仅以下场景需添加 <code>@Transactional</code>，且必须指定 <code>readOnly = true</code> 优化性能：</p><ol><li>多 SQL 关联查询（需保证数据一致性，如同时查询订单+订单详情）；</li><li>需数据库只读优化（如 MySQL 对 <code>readOnly</code> 事务的锁、日志优化）；</li><li>查询嵌套写操作（需保证整体原子性）；</li><li>依赖事务上下文的功能（如 MyBatis 延迟加载、Spring Cache 事务同步）。</li></ol><p>示例（特殊查询场景）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;OrderMapper, Order&gt; <span class="keyword">implements</span> <span class="title class_">OrderService</span> &#123;</span><br><span class="line">    <span class="comment">// 多表关联查询，需保证数据一致性，添加只读事务</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="meta">@Transactional(readOnly = true)</span></span><br><span class="line">    <span class="keyword">public</span> OrderDTO <span class="title function_">getOrderWithDetail</span><span class="params">(Long orderId)</span> &#123;</span><br><span class="line">        <span class="type">Order</span> <span class="variable">order</span> <span class="operator">=</span> <span class="built_in">this</span>.getById(orderId);</span><br><span class="line">        <span class="type">OrderDetail</span> <span class="variable">detail</span> <span class="operator">=</span> orderDetailService.getByOrderId(orderId);</span><br><span class="line">        <span class="keyword">return</span> OrderDTO.builder().order(order).detail(detail).build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="事务使用禁忌（核心）"><a href="#事务使用禁忌（核心）" class="headerlink" title="事务使用禁忌（核心）"></a>事务使用禁忌（核心）</h3><ul><li>普通单表查询**禁止添加 <code>@Transactional</code>**，否则会增加事务上下文开销、延长连接占用时间；</li><li>写操作（增删改）**必须添加 <code>@Transactional</code>**，保证数据原子性与一致性。</li></ul><h2 id="四、MyBatis-Plus-使用-ORM-接口的规范与注意事项"><a href="#四、MyBatis-Plus-使用-ORM-接口的规范与注意事项" class="headerlink" title="四、MyBatis-Plus 使用 ORM 接口的规范与注意事项"></a>四、MyBatis-Plus 使用 ORM 接口的规范与注意事项</h2><p>结合上述警告问题，延伸整理 MyBatis-Plus 中 Service 层使用 ORM 接口的行业规范，涵盖继承、调用、事务、分层等核心场景。</p><h3 id="1-Service-层继承-ServiceImpl-规范"><a href="#1-Service-层继承-ServiceImpl-规范" class="headerlink" title="1. Service 层继承 ServiceImpl 规范"></a>1. Service 层继承 <code>ServiceImpl</code> 规范</h3><h4 id="（1）规范用法（官方推荐，行业主流）"><a href="#（1）规范用法（官方推荐，行业主流）" class="headerlink" title="（1）规范用法（官方推荐，行业主流）"></a>（1）规范用法（官方推荐，行业主流）</h4><p><code>ServiceImpl&lt;M extends BaseMapper&lt;T&gt;, T&gt;</code> 是 MyBatis-Plus 提供的通用 CRUD 封装基类，继承后可通过 <code>this</code> 复用基础 ORM 方法（<code>getById</code>、<code>list</code>、<code>save</code>、<code>update</code> 等），减少冗余代码。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UserMapper, User&gt; <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="comment">// 基础查询：复用 this 方法，符合规范</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (id == <span class="literal">null</span> || id &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;用户ID不合法&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.getById(id); <span class="comment">// 复用基类 CRUD，无需手动写 Mapper 调用</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="（2）禁止行为"><a href="#（2）禁止行为" class="headerlink" title="（2）禁止行为"></a>（2）禁止行为</h4><ul><li>禁止直接使用 <code>this.baseMapper</code> 替代 <code>this</code> 方法（绕过基类封装，丢失批量事务、异常处理等能力）；</li><li>禁止 Service 层空壳化（仅转发 <code>this</code> 方法，无参数校验、业务规则等核心逻辑）。</li></ul><h3 id="2-ORM-接口调用规范（分层职责边界）"><a href="#2-ORM-接口调用规范（分层职责边界）" class="headerlink" title="2. ORM 接口调用规范（分层职责边界）"></a>2. ORM 接口调用规范（分层职责边界）</h3><h4 id="（1）Controller-层：仅调用-Service-业务方法，不直接操作-ORM"><a href="#（1）Controller-层：仅调用-Service-业务方法，不直接操作-ORM" class="headerlink" title="（1）Controller 层：仅调用 Service 业务方法，不直接操作 ORM"></a>（1）Controller 层：仅调用 Service 业务方法，不直接操作 ORM</h4><p>Controller 职责：接收请求、参数校验、响应返回，<strong>禁止直接注入 Mapper 或调用 Service 原生 ORM 方法</strong>，必须调用 Service 封装的业务方法。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 规范写法</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/&#123;id&#125;&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Result&lt;User&gt; <span class="title function_">getUserById</span><span class="params">(<span class="meta">@PathVariable</span> Long id)</span> &#123;</span><br><span class="line">        <span class="comment">// 仅做简单参数校验，调用 Service 业务方法</span></span><br><span class="line">        <span class="keyword">if</span> (id == <span class="literal">null</span> || id &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> Result.fail(<span class="string">&quot;用户ID不合法&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> userService.getUserById(id); <span class="comment">// 间接使用 ORM，符合分层规范</span></span><br><span class="line">        <span class="keyword">return</span> Result.success(user);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="（2）Service-层：ORM-调用-业务逻辑封装，事务统一控制"><a href="#（2）Service-层：ORM-调用-业务逻辑封装，事务统一控制" class="headerlink" title="（2）Service 层：ORM 调用 + 业务逻辑封装，事务统一控制"></a>（2）Service 层：ORM 调用 + 业务逻辑封装，事务统一控制</h4><p>Service 职责：封装业务规则、协调数据访问、控制事务，ORM 操作仅作为数据访问手段，不暴露给上层。</p><ul><li>写操作必须加 <code>@Transactional</code>；</li><li>普通查询不加事务，特殊查询加 <code>@Transactional(readOnly = true)</code>；</li><li>跨表查询优先依赖其他 Service（复用业务逻辑），极简场景可直接依赖 Mapper。</li></ul><h3 id="3-跨-Service-ORM-调用规范"><a href="#3-跨-Service-ORM-调用规范" class="headerlink" title="3. 跨 Service ORM 调用规范"></a>3. 跨 Service ORM 调用规范</h3><p>当 Service 需要查询其他表数据（如 OrderService 查询 User 表），遵循以下选型原则：</p><table><thead><tr><th>选型</th><th>适用场景</th><th>优势</th><th>注意事项</th></tr></thead><tbody><tr><td>依赖其他 Service（推荐）</td><td>需复用业务逻辑（校验、脱敏、权限）、多实现扩展</td><td>避免代码冗余，符合分层规范</td><td>循环依赖用 <code>@Lazy</code> 或拆分公共服务解决</td></tr><tr><td>直接依赖其他 Mapper</td><td>仅纯数据查询，无业务逻辑，无扩展需求</td><td>轻量高效，无额外依赖</td><td>禁止滥用，仅适用于极简场景</td></tr></tbody></table><p>示例（依赖其他 Service，解决循环依赖）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="meta">@RequiredArgsConstructor</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;OrderMapper, Order&gt; <span class="keyword">implements</span> <span class="title class_">OrderService</span> &#123;</span><br><span class="line">    <span class="comment">// @Lazy 延迟加载，打破循环依赖</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="meta">@Lazy</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> OrderDTO <span class="title function_">getOrderWithUser</span><span class="params">(Long orderId)</span> &#123;</span><br><span class="line">        <span class="type">Order</span> <span class="variable">order</span> <span class="operator">=</span> <span class="built_in">this</span>.getById(orderId);</span><br><span class="line">        <span class="comment">// 调用 UserService 业务方法，复用用户校验、脱敏逻辑</span></span><br><span class="line">        <span class="type">UserDTO</span> <span class="variable">user</span> <span class="operator">=</span> userService.getUserDetail(order.getUserId());</span><br><span class="line">        <span class="keyword">return</span> OrderDTO.builder().order(order).user(user).build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-简单系统-Service-层简化规范"><a href="#4-简单系统-Service-层简化规范" class="headerlink" title="4. 简单系统 Service 层简化规范"></a>4. 简单系统 Service 层简化规范</h3><p>若系统业务简单，无多实现、扩展需求，<strong>可省略 Service 接口</strong>，直接编写 Service 实现类，Controller 直接注入实现类，不影响任何功能（事务、AOP、ORM 复用均正常）。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 简化版 Service 实现类（无接口）</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserService</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UserMapper, User&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (id == <span class="literal">null</span> || id &lt;= <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;ID不合法&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.getById(id);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Transactional</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">updateUser</span><span class="params">(User user)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.updateById(user);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Controller 直接注入实现类</span></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService; <span class="comment">// 直接注入实现类，符合简单系统规范</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-缓存层结合-ORM-规范"><a href="#5-缓存层结合-ORM-规范" class="headerlink" title="5. 缓存层结合 ORM 规范"></a>5. 缓存层结合 ORM 规范</h3><p>引入缓存层时，建议采用「Service 接口 + 多实现」模式，分离<strong>纯业务实现</strong>（无缓存，操作 ORM）和<strong>缓存实现</strong>（增强业务，添加缓存逻辑），实现职责分离与灵活扩展。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. Service 接口（定义契约）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    User <span class="title function_">getUserById</span><span class="params">(Long id)</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 纯业务实现（操作 ORM，无缓存）</span></span><br><span class="line"><span class="meta">@Service(&quot;userServiceImpl&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceImpl</span> <span class="keyword">extends</span> <span class="title class_">ServiceImpl</span>&lt;UserMapper, User&gt; <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.getById(id); <span class="comment">// 仅操作数据库</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 缓存实现（增强业务，添加 Redis 缓存）</span></span><br><span class="line"><span class="meta">@Service(&quot;userServiceCacheImpl&quot;)</span></span><br><span class="line"><span class="meta">@Primary</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceCacheImpl</span> <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="meta">@Resource(name = &quot;userServiceImpl&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> UserService userServiceImpl;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> RedisTemplate&lt;String, Object&gt; redisTemplate;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(Long id)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> <span class="string">&quot;user:info:&quot;</span> + id;</span><br><span class="line">        <span class="type">User</span> <span class="variable">cacheUser</span> <span class="operator">=</span> (User) redisTemplate.opsForValue().get(key);</span><br><span class="line">        <span class="keyword">if</span> (cacheUser != <span class="literal">null</span>) <span class="keyword">return</span> cacheUser;</span><br><span class="line">        <span class="comment">// 调用纯业务实现查询数据库</span></span><br><span class="line">        <span class="type">User</span> <span class="variable">dbUser</span> <span class="operator">=</span> userServiceImpl.getUserById(id);</span><br><span class="line">        <span class="keyword">if</span> (dbUser != <span class="literal">null</span>) redisTemplate.opsForValue().set(key, dbUser, <span class="number">86400L</span>, TimeUnit.SECONDS);</span><br><span class="line">        <span class="keyword">return</span> dbUser;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="五、核心规范总结"><a href="#五、核心规范总结" class="headerlink" title="五、核心规范总结"></a>五、核心规范总结</h2><ol><li>事务警告日志：普通查询无需处理，直接忽略；写操作必须加 <code>@Transactional</code>，特殊查询加 <code>@Transactional(readOnly = true)</code>；</li><li>MyBatis-Plus ORM 复用：优先继承 <code>ServiceImpl</code>，通过 <code>this</code> 调用基础 CRUD，禁止直接滥用 <code>baseMapper</code>；</li><li>分层职责：Controller 不直接操作 ORM，Service 封装业务与事务，跨表查询优先依赖其他 Service；</li><li>简单系统：可省略 Service 接口，直接用实现类，不影响功能与规范；</li><li>缓存扩展：采用接口+多实现模式，分离业务与缓存逻辑，提升可维护性。</li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> spring </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>完整Docker部署全流程教程（Windows本地 → Ubuntu服务器）</title>
      <link href="/2026/01/14/%E5%AE%8C%E6%95%B4Docker%E9%83%A8%E7%BD%B2%E5%85%A8%E6%B5%81%E7%A8%8B%E6%95%99%E7%A8%8B%EF%BC%88Windows%E6%9C%AC%E5%9C%B0%20%E2%86%92%20Ubuntu%E6%9C%8D%E5%8A%A1%E5%99%A8%EF%BC%89/"/>
      <url>/2026/01/14/%E5%AE%8C%E6%95%B4Docker%E9%83%A8%E7%BD%B2%E5%85%A8%E6%B5%81%E7%A8%8B%E6%95%99%E7%A8%8B%EF%BC%88Windows%E6%9C%AC%E5%9C%B0%20%E2%86%92%20Ubuntu%E6%9C%8D%E5%8A%A1%E5%99%A8%EF%BC%89/</url>
      
        <content type="html"><![CDATA[<h1 id="完整Docker部署全流程教程（Windows本地-→-Ubuntu服务器）"><a href="#完整Docker部署全流程教程（Windows本地-→-Ubuntu服务器）" class="headerlink" title="完整Docker部署全流程教程（Windows本地 → Ubuntu服务器）"></a>完整Docker部署全流程教程（Windows本地 → Ubuntu服务器）</h1><p>这份教程整合了从Docker安装到最终在Ubuntu服务器运行的全流程，涵盖MySQL（含初始化SQL）、Redis、SpringBoot容器的构建、互通、打包迁移，同时标注了你之前踩过的所有坑，确保一步到位。</p><h2 id="一、前置说明"><a href="#一、前置说明" class="headerlink" title="一、前置说明"></a>一、前置说明</h2><ul><li><strong>适用场景</strong>：Windows本地开发调试Docker容器，迁移到Ubuntu服务器稳定运行</li><li><strong>核心目标</strong>：实现MySQL（含初始化SQL）、Redis、SpringBoot容器互通，迁移后无需重复配置</li><li><strong>工具准备</strong>：<ul><li>Windows：Docker Desktop、PowerShell（管理员模式）、Maven（打包SpringBoot）</li><li>Ubuntu服务器：已安装SSH、开放22&#x2F;8080&#x2F;3306&#x2F;6379端口</li><li>网络：Windows能访问Ubuntu服务器（公网&#x2F;内网）</li></ul></li></ul><h2 id="二、阶段1：Windows本地环境准备（解决镜像源坑）"><a href="#二、阶段1：Windows本地环境准备（解决镜像源坑）" class="headerlink" title="二、阶段1：Windows本地环境准备（解决镜像源坑）"></a>二、阶段1：Windows本地环境准备（解决镜像源坑）</h2><h3 id="1-安装Docker-Desktop"><a href="#1-安装Docker-Desktop" class="headerlink" title="1. 安装Docker Desktop"></a>1. 安装Docker Desktop</h3><ul><li>下载地址：<a href="https://www.docker.com/products/docker-desktop/">https://www.docker.com/products/docker-desktop/</a></li><li>安装后启动，等待右下角Docker图标显示「Running」（约10秒）。</li></ul><h3 id="2-配置国内镜像源（解决拉取镜像超时-x2F-403）"><a href="#2-配置国内镜像源（解决拉取镜像超时-x2F-403）" class="headerlink" title="2. 配置国内镜像源（解决拉取镜像超时&#x2F;403）"></a>2. 配置国内镜像源（解决拉取镜像超时&#x2F;403）</h3><blockquote><p>❗ 坑点1：直接拉取Docker Hub镜像会超时&#x2F;403，必须配置国内源<br> 打开Docker Desktop → <code>Settings</code> → <code>Docker Engine</code>；</p></blockquote><h2 id="三、阶段2：Windows本地拉取基础镜像（MySQL-x2F-Redis）"><a href="#三、阶段2：Windows本地拉取基础镜像（MySQL-x2F-Redis）" class="headerlink" title="三、阶段2：Windows本地拉取基础镜像（MySQL&#x2F;Redis）"></a>三、阶段2：Windows本地拉取基础镜像（MySQL&#x2F;Redis）</h2><p>打开Windows PowerShell（管理员模式），执行以下命令：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 拉取MySQL 8.0镜像（稳定版）</span></span><br><span class="line">docker pull mysql:8.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取Redis最新版镜像</span></span><br><span class="line">docker pull redis:latest</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点2：若仍拉取失败 → 确认镜像源配置生效，或后续用「手动导入镜像包」兜底。</p></blockquote><h2 id="四、阶段3：Windows本地配置MySQL容器（含初始化SQL-数据持久化）"><a href="#四、阶段3：Windows本地配置MySQL容器（含初始化SQL-数据持久化）" class="headerlink" title="四、阶段3：Windows本地配置MySQL容器（含初始化SQL+数据持久化）"></a>四、阶段3：Windows本地配置MySQL容器（含初始化SQL+数据持久化）</h2><h3 id="1-准备本地目录（映射数据-初始化SQL）"><a href="#1-准备本地目录（映射数据-初始化SQL）" class="headerlink" title="1. 准备本地目录（映射数据+初始化SQL）"></a>1. 准备本地目录（映射数据+初始化SQL）</h3><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建MySQL数据/初始化SQL目录</span></span><br><span class="line">mkdir <span class="literal">-p</span> D:\docker<span class="literal">-mysql</span>\data</span><br><span class="line">mkdir <span class="literal">-p</span> D:\docker<span class="literal">-mysql</span>\sql</span><br></pre></td></tr></table></figure><ul><li>将项目的初始SQL文件（如<code>init.sql</code>）复制到 <code>D:\docker-mysql\sql</code> 目录。</li></ul><h3 id="2-创建自定义网络（解决容器互通坑）"><a href="#2-创建自定义网络（解决容器互通坑）" class="headerlink" title="2. 创建自定义网络（解决容器互通坑）"></a>2. 创建自定义网络（解决容器互通坑）</h3><blockquote><p>❗ 坑点3：默认bridge网络无法解析容器名，必须创建自定义网络</p></blockquote><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建专属网络（所有容器接入此网络）</span></span><br><span class="line">docker network create my-app-network</span><br></pre></td></tr></table></figure><h3 id="3-启动MySQL容器（一键初始化）"><a href="#3-启动MySQL容器（一键初始化）" class="headerlink" title="3. 启动MySQL容器（一键初始化）"></a>3. 启动MySQL容器（一键初始化）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name mysql-container \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 3306:3306 \</span><br><span class="line">  -v D:\docker-mysql\data:/var/lib/mysql \  <span class="comment"># 数据持久化（关键！避免容器删了数据丢）</span></span><br><span class="line">  -v D:\docker-mysql\sql:/docker-entrypoint-initdb.d \  <span class="comment"># 自动导入init.sql</span></span><br><span class="line">  -e MYSQL_ROOT_PASSWORD=123456 \  <span class="comment"># root密码</span></span><br><span class="line">  -e MYSQL_DATABASE=your_db \  <span class="comment"># 初始化创建的数据库名</span></span><br><span class="line">  -e MYSQL_CHARACTER_SET_SERVER=utf8mb4 \  <span class="comment"># 解决中文乱码</span></span><br><span class="line">  -e MYSQL_COLLATION_SERVER=utf8mb4_unicode_ci \</span><br><span class="line">  -e TZ=Asia/Shanghai \  <span class="comment"># 时区同步（避免时间错乱）</span></span><br><span class="line">  mysql:8.0 \</span><br><span class="line">  --default-authentication-plugin=mysql_native_password  <span class="comment"># 兼容SpringBoot连接</span></span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点4：初始化SQL仅首次启动生效 → 若已启动过容器，需先删除<code>D:\docker-mysql\data</code>目录再重启；<br>❗ 坑点5：容器内<code>localhost</code>不是宿主机 → 后续SpringBoot配置中MySQL host填容器名（<code>mysql-container</code>）。</p></blockquote><h3 id="4-验证MySQL容器（确认SQL导入成功）"><a href="#4-验证MySQL容器（确认SQL导入成功）" class="headerlink" title="4. 验证MySQL容器（确认SQL导入成功）"></a>4. 验证MySQL容器（确认SQL导入成功）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入MySQL容器</span></span><br><span class="line">docker <span class="built_in">exec</span> -it mysql-container /bin/bash</span><br><span class="line"></span><br><span class="line"><span class="comment"># 登录MySQL（输入密码123456）</span></span><br><span class="line">mysql -uroot -p</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证数据库和表</span></span><br><span class="line">use your_db;</span><br><span class="line">show tables;  <span class="comment"># 能看到init.sql中的表则成功</span></span><br></pre></td></tr></table></figure><h2 id="五、阶段4：Windows本地配置Redis容器"><a href="#五、阶段4：Windows本地配置Redis容器" class="headerlink" title="五、阶段4：Windows本地配置Redis容器"></a>五、阶段4：Windows本地配置Redis容器</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name redislocal \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 6379:6379 \</span><br><span class="line">  redis:latest \</span><br><span class="line">  --<span class="built_in">bind</span> 0.0.0.0 --protected-mode no  <span class="comment"># 关闭保护模式，允许容器访问</span></span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点6：Redis无法访问 → 必须加<code>--bind 0.0.0.0 --protected-mode no</code>。</p></blockquote><h2 id="六、阶段5：Windows本地构建SpringBoot镜像"><a href="#六、阶段5：Windows本地构建SpringBoot镜像" class="headerlink" title="六、阶段5：Windows本地构建SpringBoot镜像"></a>六、阶段5：Windows本地构建SpringBoot镜像</h2><h3 id="1-准备SpringBoot项目"><a href="#1-准备SpringBoot项目" class="headerlink" title="1. 准备SpringBoot项目"></a>1. 准备SpringBoot项目</h3><h4 id="（1）固定JAR包名称（避免Dockerfile找不到JAR）"><a href="#（1）固定JAR包名称（避免Dockerfile找不到JAR）" class="headerlink" title="（1）固定JAR包名称（避免Dockerfile找不到JAR）"></a>（1）固定JAR包名称（避免Dockerfile找不到JAR）</h4><p>修改项目<code>pom.xml</code>：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">finalName</span>&gt;</span>app<span class="tag">&lt;/<span class="name">finalName</span>&gt;</span>  <span class="comment">&lt;!-- 打包后生成target/app.jar --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure><h4 id="（2）修改配置文件（关键：容器名访问MySQL-x2F-Redis）"><a href="#（2）修改配置文件（关键：容器名访问MySQL-x2F-Redis）" class="headerlink" title="（2）修改配置文件（关键：容器名访问MySQL&#x2F;Redis）"></a>（2）修改配置文件（关键：容器名访问MySQL&#x2F;Redis）</h4><p>修改<code>application.yml</code>（删除冗余配置，核心如下）：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="comment"># 连接MySQL容器（host填容器名，不是localhost）</span></span><br><span class="line">  <span class="attr">datasource:</span></span><br><span class="line">    <span class="attr">url:</span> <span class="string">jdbc:mysql://mysql-container:3306/your_db?useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true</span></span><br><span class="line">    <span class="attr">username:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">password:</span> <span class="number">123456</span></span><br><span class="line">    <span class="attr">driver-class-name:</span> <span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line">  <span class="comment"># 连接Redis容器（host填容器名）</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">host:</span> <span class="string">redislocal</span></span><br><span class="line">    <span class="attr">port:</span> <span class="number">6379</span></span><br><span class="line">  <span class="comment"># 生产环境配置（也可启动时指定）</span></span><br><span class="line">  <span class="attr">profiles:</span></span><br><span class="line">    <span class="attr">active:</span> <span class="string">prod</span></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8080</span></span><br></pre></td></tr></table></figure><h3 id="2-打包SpringBoot项目"><a href="#2-打包SpringBoot项目" class="headerlink" title="2. 打包SpringBoot项目"></a>2. 打包SpringBoot项目</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入项目根目录（如D:\your-springboot-project）</span></span><br><span class="line"><span class="built_in">cd</span> D:\your-springboot-project</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打包（跳过测试，加快速度）</span></span><br><span class="line">mvn clean package -DskipTests</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点7：打包失败 → 确认Maven环境正常，项目无语法错误。</p></blockquote><h3 id="3-编写Dockerfile（放在项目根目录）"><a href="#3-编写Dockerfile（放在项目根目录）" class="headerlink" title="3. 编写Dockerfile（放在项目根目录）"></a>3. 编写Dockerfile（放在项目根目录）</h3><blockquote><p>❗ 坑点8：避免用Ubuntu镜像+Supervisor（复杂易报错），直接用纯JDK镜像</p></blockquote><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 用国内华为云源的OpenJDK 17，避免Docker Hub 403</span></span><br><span class="line"><span class="keyword">FROM</span> swr.cn-east-<span class="number">3</span>.myhuaweicloud.com/opensourceway/openjdk:<span class="number">17</span>-jdk-slim</span><br><span class="line"></span><br><span class="line"><span class="comment"># 环境配置（时区+端口+JVM参数）</span></span><br><span class="line"><span class="keyword">ENV</span> TZ=Asia/Shanghai</span><br><span class="line"><span class="keyword">ENV</span> SERVER_PORT=<span class="number">8080</span></span><br><span class="line"><span class="keyword">ENV</span> JAVA_OPTS=<span class="string">&quot;-Xms512m -Xmx1024m -Djava.security.egd=file:/dev/./urandom -Duser.timezone=Asia/Shanghai&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置时区</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> <span class="built_in">ln</span> -snf /usr/share/zoneinfo/<span class="variable">$TZ</span> /etc/localtime &amp;&amp; <span class="built_in">echo</span> <span class="variable">$TZ</span> &gt; /etc/timezone</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署项目</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="language-bash"> /app</span></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> target/app.jar /app/app.jar</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 暴露端口</span></span><br><span class="line"><span class="keyword">EXPOSE</span> $&#123;SERVER_PORT&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动命令</span></span><br><span class="line"><span class="keyword">ENTRYPOINT</span><span class="language-bash"> [<span class="string">&quot;sh&quot;</span>, <span class="string">&quot;-c&quot;</span>, <span class="string">&quot;java <span class="variable">$JAVA_OPTS</span> -jar /app/app.jar --server.port=<span class="variable">$&#123;SERVER_PORT&#125;</span>&quot;</span>]</span></span><br></pre></td></tr></table></figure><h3 id="4-构建SpringBoot镜像"><a href="#4-构建SpringBoot镜像" class="headerlink" title="4. 构建SpringBoot镜像"></a>4. 构建SpringBoot镜像</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 项目根目录执行</span></span><br><span class="line">docker build -t sb-booking:v1 .</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点9：提示<code>app.jar找不到</code> → 确认<code>mvn package</code>成功，target目录下有<code>app.jar</code>。</p></blockquote><h3 id="5-启动SpringBoot容器（验证互通）"><a href="#5-启动SpringBoot容器（验证互通）" class="headerlink" title="5. 启动SpringBoot容器（验证互通）"></a>5. 启动SpringBoot容器（验证互通）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name sb-booking \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 8080:8080 \</span><br><span class="line">  -e spring.profiles.active=prod \  <span class="comment"># 启动时指定生产环境（优先级最高）</span></span><br><span class="line">  sb-booking:v1</span><br></pre></td></tr></table></figure><h3 id="6-验证SpringBoot容器"><a href="#6-验证SpringBoot容器" class="headerlink" title="6. 验证SpringBoot容器"></a>6. 验证SpringBoot容器</h3><ul><li>访问<code>http://localhost:8080</code>（能打开项目接口文档&#x2F;首页则启动成功）；</li><li>调用接口验证：确认能读写MySQL&#x2F;Redis（无连接错误则互通成功）。</li></ul><h2 id="七、阶段6：Windows本地打包容器为镜像（避免重复配置）"><a href="#七、阶段6：Windows本地打包容器为镜像（避免重复配置）" class="headerlink" title="七、阶段6：Windows本地打包容器为镜像（避免重复配置）"></a>七、阶段6：Windows本地打包容器为镜像（避免重复配置）</h2><p>将已配置好的MySQL容器保存为自定义镜像（含初始化SQL、环境变量），迁移后无需重复配置：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># （可选）停止MySQL容器（更稳定）</span></span><br><span class="line">docker stop mysql-container</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交容器为新镜像（快照）</span></span><br><span class="line">docker commit mysql-container mysql-custom:v1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动MySQL容器（可选）</span></span><br><span class="line">docker start mysql-container</span><br></pre></td></tr></table></figure><h3 id="导出镜像为tar包（迁移用）"><a href="#导出镜像为tar包（迁移用）" class="headerlink" title="导出镜像为tar包（迁移用）"></a>导出镜像为tar包（迁移用）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建镜像导出目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p D:\docker-images</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导出MySQL自定义镜像</span></span><br><span class="line">docker save -o D:\docker-images\mysql-custom-v1.tar mysql-custom:v1</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导出SpringBoot镜像</span></span><br><span class="line">docker save -o D:\docker-images\sb-booking-v1.tar sb-booking:v1</span><br><span class="line"></span><br><span class="line"><span class="comment"># （可选）导出Redis镜像</span></span><br><span class="line">docker save -o D:\docker-images\redis-custom-v1.tar redis:latest</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点10：镜像体积过大 → 导出前可清理容器日志（<code>docker exec -it 容器名 rm -rf /var/log/*.log</code>），或压缩为<code>tar.gz</code>。</p></blockquote><h2 id="八、阶段7：传输tar包到Ubuntu服务器"><a href="#八、阶段7：传输tar包到Ubuntu服务器" class="headerlink" title="八、阶段7：传输tar包到Ubuntu服务器"></a>八、阶段7：传输tar包到Ubuntu服务器</h2><h3 id="1-先给Ubuntu安装Docker（若未安装）"><a href="#1-先给Ubuntu安装Docker（若未安装）" class="headerlink" title="1. 先给Ubuntu安装Docker（若未安装）"></a>1. 先给Ubuntu安装Docker（若未安装）</h3><p>登录Ubuntu服务器（SSH），执行：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 更新源</span></span><br><span class="line">sudo apt update</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装Docker</span></span><br><span class="line">sudo apt install -y docker.io</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动并开机自启</span></span><br><span class="line">sudo systemctl start docker</span><br><span class="line">sudo systemctl <span class="built_in">enable</span> docker</span><br><span class="line"></span><br><span class="line"><span class="comment"># 给普通用户Docker权限（避免每次输sudo）</span></span><br><span class="line">sudo usermod -aG docker <span class="variable">$USER</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重启SSH会话生效（退出再重新登录）</span></span><br></pre></td></tr></table></figure><h3 id="2-Windows传输tar包到Ubuntu（SCP命令）"><a href="#2-Windows传输tar包到Ubuntu（SCP命令）" class="headerlink" title="2. Windows传输tar包到Ubuntu（SCP命令）"></a>2. Windows传输tar包到Ubuntu（SCP命令）</h3><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Windows PowerShell执行（替换服务器IP/用户名）</span></span><br><span class="line"><span class="comment"># 传输MySQL镜像</span></span><br><span class="line">scp D:\docker<span class="literal">-images</span>\mysql<span class="literal">-custom-v1</span>.tar root<span class="selector-tag">@</span>你的服务器IP:/usr/local/docker<span class="literal">-images</span>/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 传输SpringBoot镜像</span></span><br><span class="line">scp D:\docker<span class="literal">-images</span>\sb<span class="literal">-booking-v1</span>.tar root<span class="selector-tag">@</span>你的服务器IP:/usr/local/docker<span class="literal">-images</span>/</span><br><span class="line"></span><br><span class="line"><span class="comment"># （可选）传输Redis镜像</span></span><br><span class="line">scp D:\docker<span class="literal">-images</span>\redis<span class="literal">-custom-v1</span>.tar root<span class="selector-tag">@</span>你的服务器IP:/usr/local/docker<span class="literal">-images</span>/</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点11：SCP传输超时 → 确认服务器防火墙开放22端口，网络稳定。</p></blockquote><h2 id="九、阶段8：Ubuntu服务器导入并运行镜像"><a href="#九、阶段8：Ubuntu服务器导入并运行镜像" class="headerlink" title="九、阶段8：Ubuntu服务器导入并运行镜像"></a>九、阶段8：Ubuntu服务器导入并运行镜像</h2><h3 id="1-登录Ubuntu服务器，创建目录"><a href="#1-登录Ubuntu服务器，创建目录" class="headerlink" title="1. 登录Ubuntu服务器，创建目录"></a>1. 登录Ubuntu服务器，创建目录</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建镜像/数据目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /usr/local/docker-images</span><br><span class="line"><span class="built_in">mkdir</span> -p /usr/local/docker-mysql/data</span><br></pre></td></tr></table></figure><h3 id="2-导入镜像"><a href="#2-导入镜像" class="headerlink" title="2. 导入镜像"></a>2. 导入镜像</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 导入MySQL自定义镜像</span></span><br><span class="line">docker load -i /usr/local/docker-images/mysql-custom-v1.tar</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导入SpringBoot镜像</span></span><br><span class="line">docker load -i /usr/local/docker-images/sb-booking-v1.tar</span><br><span class="line"></span><br><span class="line"><span class="comment"># （可选）导入Redis镜像</span></span><br><span class="line">docker load -i /usr/local/docker-images/redis-custom-v1.tar</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点12：导入后镜像名显示<code>&lt;none&gt;</code> → 手动打标签：<code>docker tag 镜像ID mysql-custom:v1</code>（镜像ID用<code>docker images</code>查看）。</p></blockquote><h3 id="3-创建自定义网络（和本地一致）"><a href="#3-创建自定义网络（和本地一致）" class="headerlink" title="3. 创建自定义网络（和本地一致）"></a>3. 创建自定义网络（和本地一致）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker network create my-app-network</span><br></pre></td></tr></table></figure><h3 id="4-运行MySQL容器（无需冗长配置）"><a href="#4-运行MySQL容器（无需冗长配置）" class="headerlink" title="4. 运行MySQL容器（无需冗长配置）"></a>4. 运行MySQL容器（无需冗长配置）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name mysql-container \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 3306:3306 \</span><br><span class="line">  -v /usr/local/docker-mysql/data:/var/lib/mysql \  <span class="comment"># 数据持久化</span></span><br><span class="line">  --restart=always \  <span class="comment"># 开机自启</span></span><br><span class="line">  mysql-custom:v1</span><br></pre></td></tr></table></figure><h3 id="5-运行Redis容器（Ubuntu）"><a href="#5-运行Redis容器（Ubuntu）" class="headerlink" title="5. 运行Redis容器（Ubuntu）"></a>5. 运行Redis容器（Ubuntu）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name redislocal \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 6379:6379 \</span><br><span class="line">  --restart=always \</span><br><span class="line">  redis:latest \</span><br><span class="line">  --<span class="built_in">bind</span> 0.0.0.0 --protected-mode no</span><br></pre></td></tr></table></figure><h3 id="6-运行SpringBoot容器（Ubuntu）"><a href="#6-运行SpringBoot容器（Ubuntu）" class="headerlink" title="6. 运行SpringBoot容器（Ubuntu）"></a>6. 运行SpringBoot容器（Ubuntu）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name sb-booking \</span><br><span class="line">  --network my-app-network \</span><br><span class="line">  -p 8080:8080 \</span><br><span class="line">  -e spring.profiles.active=prod \</span><br><span class="line">  --restart=always \  <span class="comment"># 异常重启</span></span><br><span class="line">  sb-booking:v1</span><br></pre></td></tr></table></figure><h3 id="7-验证运行状态"><a href="#7-验证运行状态" class="headerlink" title="7. 验证运行状态"></a>7. 验证运行状态</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看容器状态（全部Up则成功）</span></span><br><span class="line">docker ps</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看SpringBoot日志（无报错则正常）</span></span><br><span class="line">docker logs sb-booking</span><br><span class="line"></span><br><span class="line"><span class="comment"># 验证端口可访问</span></span><br><span class="line">curl http://localhost:8080</span><br></pre></td></tr></table></figure><blockquote><p>❗ 坑点13：服务器外网无法访问8080&#x2F;3306 → 开放防火墙端口：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo ufw allow 8080/tcp</span><br><span class="line">sudo ufw allow 3306/tcp</span><br><span class="line">sudo ufw reload</span><br></pre></td></tr></table></figure><p>（云服务器需额外在安全组开放端口）</p></blockquote><h2 id="十、全流程核心踩坑总结（必看！）"><a href="#十、全流程核心踩坑总结（必看！）" class="headerlink" title="十、全流程核心踩坑总结（必看！）"></a>十、全流程核心踩坑总结（必看！）</h2><ol><li><strong>镜像源问题</strong>：Docker Hub拉取超时&#x2F;403 → 配置中科大&#x2F;网易镜像源，或用国内厂商镜像（如华为云OpenJDK）；</li><li><strong>容器互通问题</strong>：默认bridge网络无法解析容器名 → 创建自定义bridge网络，所有容器接入；</li><li><strong>MySQL初始化问题</strong>：SQL仅首次启动生效 → 首次启动前确保数据目录为空；</li><li><strong>地址配置问题</strong>：容器内<code>localhost</code>不是宿主机 → SpringBoot配置中MySQL&#x2F;Redis host填容器名；</li><li><strong>数据持久化问题</strong>：容器删除数据丢失 → 必须挂载宿主机目录到<code>/var/lib/mysql</code>；</li><li><strong>端口问题</strong>：容器创建后无法改端口 → 重建容器（推荐），避免修改Docker配置文件；</li><li><strong>环境变量优先级</strong>：<code>docker run -e</code> &gt; Dockerfile ENV &gt; 配置文件 → 生产环境优先用启动参数指定；</li><li><strong>架构兼容问题</strong>：Windows打包的x86镜像不能在ARM服务器运行 → 打包时加<code>--platform linux/amd64</code>；</li><li><strong>Ubuntu权限问题</strong>：运行Docker需sudo → 添加用户到docker组；</li><li><strong>端口访问问题</strong>：服务器容器端口无法访问 → 开放防火墙+云服务器安全组端口。</li></ol><h2 id="十一、最终验证"><a href="#十一、最终验证" class="headerlink" title="十一、最终验证"></a>十一、最终验证</h2><p>在Ubuntu服务器外网访问<code>http://服务器IP:8080</code>，调用接口验证MySQL&#x2F;Redis读写正常，即全流程部署成功。</p>]]></content>
      
      
      
        <tags>
            
            <tag> docker </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>纯命令行完整部署流程docker compose</title>
      <link href="/2026/01/13/docker%20compose%20%E9%A1%B9%E7%9B%AE%E9%83%A8%E7%BD%B2%E6%B5%81%E7%A8%8B/"/>
      <url>/2026/01/13/docker%20compose%20%E9%A1%B9%E7%9B%AE%E9%83%A8%E7%BD%B2%E6%B5%81%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<h1 id="✅-纯命令行完整部署流程（无图形化-x2F-无Portainer）"><a href="#✅-纯命令行完整部署流程（无图形化-x2F-无Portainer）" class="headerlink" title="✅ 纯命令行完整部署流程（无图形化&#x2F;无Portainer）"></a>✅ 纯命令行完整部署流程（无图形化&#x2F;无Portainer）</h1><h2 id="适用环境：Ubuntu服务器、无防火墙-x2F-安全组权限、80端口可用、本地原生Nginx、多SpringBoot项目（前后端分离）、域名访问、纯Docker命令行操作"><a href="#适用环境：Ubuntu服务器、无防火墙-x2F-安全组权限、80端口可用、本地原生Nginx、多SpringBoot项目（前后端分离）、域名访问、纯Docker命令行操作" class="headerlink" title="适用环境：Ubuntu服务器、无防火墙&#x2F;安全组权限、80端口可用、本地原生Nginx、多SpringBoot项目（前后端分离）、域名访问、纯Docker命令行操作"></a>适用环境：Ubuntu服务器、无防火墙&#x2F;安全组权限、80端口可用、本地原生Nginx、多SpringBoot项目（前后端分离）、域名访问、纯Docker命令行操作</h2><h2 id="核心原则：全程-root-用户执行、所有命令复制即用、每步带【成功校验】、多项目彻底隔离无冲突、最终实现「域名-路径」访问所有项目"><a href="#核心原则：全程-root-用户执行、所有命令复制即用、每步带【成功校验】、多项目彻底隔离无冲突、最终实现「域名-路径」访问所有项目" class="headerlink" title="核心原则：全程 root 用户执行、所有命令复制即用、每步带【成功校验】、多项目彻底隔离无冲突、最终实现「域名+路径」访问所有项目"></a>核心原则：全程 root 用户执行、所有命令复制即用、每步带【成功校验】、多项目彻底隔离无冲突、最终实现「域名+路径」访问所有项目</h2><hr><h2 id="✅-整体流程划分（5大核心流程，循序渐进，无任何冗余步骤）"><a href="#✅-整体流程划分（5大核心流程，循序渐进，无任何冗余步骤）" class="headerlink" title="✅ 整体流程划分（5大核心流程，循序渐进，无任何冗余步骤）"></a>✅ 整体流程划分（5大核心流程，循序渐进，无任何冗余步骤）</h2><h3 id="流程一：服务器基础环境初始化（安装Docker-Docker-Compose-本地Nginx，必装依赖）"><a href="#流程一：服务器基础环境初始化（安装Docker-Docker-Compose-本地Nginx，必装依赖）" class="headerlink" title="流程一：服务器基础环境初始化（安装Docker+Docker-Compose+本地Nginx，必装依赖）"></a>流程一：服务器基础环境初始化（安装Docker+Docker-Compose+本地Nginx，必装依赖）</h3><h3 id="流程二：标准化目录结构搭建（多项目隔离、前后端分离，统一目录规范，避免混乱）"><a href="#流程二：标准化目录结构搭建（多项目隔离、前后端分离，统一目录规范，避免混乱）" class="headerlink" title="流程二：标准化目录结构搭建（多项目隔离、前后端分离，统一目录规范，避免混乱）"></a>流程二：标准化目录结构搭建（多项目隔离、前后端分离，统一目录规范，避免混乱）</h3><h3 id="流程三：Docker容器化部署（SpringBoot后端-MySQL-Redis，纯docker-compose命令，持久化配置，本机通信无端口暴露）"><a href="#流程三：Docker容器化部署（SpringBoot后端-MySQL-Redis，纯docker-compose命令，持久化配置，本机通信无端口暴露）" class="headerlink" title="流程三：Docker容器化部署（SpringBoot后端+MySQL+Redis，纯docker-compose命令，持久化配置，本机通信无端口暴露）"></a>流程三：Docker容器化部署（SpringBoot后端+MySQL+Redis，纯docker-compose命令，持久化配置，本机通信无端口暴露）</h3><h3 id="流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目路由隔离、前后端请求转发，彻底解决跳转-x2F-跨域-x2F-访问问题）"><a href="#流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目路由隔离、前后端请求转发，彻底解决跳转-x2F-跨域-x2F-访问问题）" class="headerlink" title="流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目路由隔离、前后端请求转发，彻底解决跳转&#x2F;跨域&#x2F;访问问题）"></a>流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目路由隔离、前后端请求转发，彻底解决跳转&#x2F;跨域&#x2F;访问问题）</h3><h3 id="流程五：域名解析-最终访问校验-运维常用命令（生产级维护，一键启停-x2F-日志查看-x2F-更新项目）"><a href="#流程五：域名解析-最终访问校验-运维常用命令（生产级维护，一键启停-x2F-日志查看-x2F-更新项目）" class="headerlink" title="流程五：域名解析+最终访问校验+运维常用命令（生产级维护，一键启停&#x2F;日志查看&#x2F;更新项目）"></a>流程五：域名解析+最终访问校验+运维常用命令（生产级维护，一键启停&#x2F;日志查看&#x2F;更新项目）</h3><hr><h2 id="—-流程一：服务器基础环境初始化（基础依赖全装完，后续所有操作的前提）"><a href="#—-流程一：服务器基础环境初始化（基础依赖全装完，后续所有操作的前提）" class="headerlink" title="— 流程一：服务器基础环境初始化（基础依赖全装完，后续所有操作的前提）"></a>— 流程一：服务器基础环境初始化（基础依赖全装完，后续所有操作的前提）</h2><h3 id="核心说明：Ubuntu系统默认无Docker-x2F-Docker-Compose，Nginx按需安装；所有命令均为Ubuntu专属，无需任何图形化操作"><a href="#核心说明：Ubuntu系统默认无Docker-x2F-Docker-Compose，Nginx按需安装；所有命令均为Ubuntu专属，无需任何图形化操作" class="headerlink" title="核心说明：Ubuntu系统默认无Docker&#x2F;Docker-Compose，Nginx按需安装；所有命令均为Ubuntu专属，无需任何图形化操作"></a>核心说明：Ubuntu系统默认无Docker&#x2F;Docker-Compose，Nginx按需安装；所有命令均为Ubuntu专属，无需任何图形化操作</h3><h3 id="1-1-更新服务器系统源-安装基础依赖"><a href="#1-1-更新服务器系统源-安装基础依赖" class="headerlink" title="1.1 更新服务器系统源+安装基础依赖"></a>1.1 更新服务器系统源+安装基础依赖</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt update &amp;&amp; apt upgrade -y</span><br><span class="line">apt install -y vim net-tools curl wget</span><br></pre></td></tr></table></figure><h3 id="1-2-卸载系统旧版本Docker（如有），避免冲突"><a href="#1-2-卸载系统旧版本Docker（如有），避免冲突" class="headerlink" title="1.2 卸载系统旧版本Docker（如有），避免冲突"></a>1.2 卸载系统旧版本Docker（如有），避免冲突</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt remove -y docker docker-engine docker.io containerd runc</span><br></pre></td></tr></table></figure><h3 id="1-3-安装Docker官方稳定版-配置开机自启"><a href="#1-3-安装Docker官方稳定版-配置开机自启" class="headerlink" title="1.3 安装Docker官方稳定版+配置开机自启"></a>1.3 安装Docker官方稳定版+配置开机自启</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装Docker依赖</span></span><br><span class="line">apt install -y ca-certificates curl gnupg lsb-release</span><br><span class="line"><span class="comment"># 添加Docker官方密钥</span></span><br><span class="line">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg</span><br><span class="line"><span class="comment"># 配置Docker源</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;deb [arch=<span class="subst">$(dpkg --print-architecture)</span> signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu <span class="subst">$(lsb_release -cs)</span> stable&quot;</span> | <span class="built_in">tee</span> /etc/apt/sources.list.d/docker.list &gt; /dev/null</span><br><span class="line"><span class="comment"># 安装Docker核心组件</span></span><br><span class="line">apt update &amp;&amp; apt install -y docker-ce docker-ce-cli containerd.io</span><br><span class="line"><span class="comment"># 启动Docker+开机自启</span></span><br><span class="line">systemctl start docker &amp;&amp; systemctl <span class="built_in">enable</span> docker</span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验1-3】Docker安装成功"><a href="#✅-【成功校验1-3】Docker安装成功" class="headerlink" title="✅ 【成功校验1.3】Docker安装成功"></a>✅ 【成功校验1.3】Docker安装成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker --version &amp;&amp; docker run hello-world</span><br></pre></td></tr></table></figure><ul><li>成功标志：打印Docker版本号 + 输出 <code>Hello from Docker!</code> 相关内容，无报错</li></ul><h3 id="1-4-安装Docker-Compose（容器编排必备，管理多容器项目）"><a href="#1-4-安装Docker-Compose（容器编排必备，管理多容器项目）" class="headerlink" title="1.4 安装Docker-Compose（容器编排必备，管理多容器项目）"></a>1.4 安装Docker-Compose（容器编排必备，管理多容器项目）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 下载稳定版docker-compose</span></span><br><span class="line">curl -L <span class="string">&quot;https://github.com/docker/compose/releases/download/v2.24.6/docker-compose-<span class="subst">$(uname -s)</span>-<span class="subst">$(uname -m)</span>&quot;</span> -o /usr/local/bin/docker-compose</span><br><span class="line"><span class="comment"># 赋予执行权限</span></span><br><span class="line"><span class="built_in">chmod</span> +x /usr/local/bin/docker-compose</span><br><span class="line"><span class="comment"># 建立软链接</span></span><br><span class="line"><span class="built_in">ln</span> -s /usr/local/bin/docker-compose /usr/bin/docker-compose</span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验1-4】Docker-Compose安装成功"><a href="#✅-【成功校验1-4】Docker-Compose安装成功" class="headerlink" title="✅ 【成功校验1.4】Docker-Compose安装成功"></a>✅ 【成功校验1.4】Docker-Compose安装成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker-compose --version</span><br></pre></td></tr></table></figure><ul><li>成功标志：打印docker-compose版本号，无报错</li></ul><h3 id="1-5-安装本地原生Nginx（80端口唯一入口，核心转发组件，服务器必备）"><a href="#1-5-安装本地原生Nginx（80端口唯一入口，核心转发组件，服务器必备）" class="headerlink" title="1.5 安装本地原生Nginx（80端口唯一入口，核心转发组件，服务器必备）"></a>1.5 安装本地原生Nginx（80端口唯一入口，核心转发组件，服务器必备）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">apt install -y nginx</span><br><span class="line"><span class="comment"># 启动Nginx+开机自启</span></span><br><span class="line">systemctl start nginx &amp;&amp; systemctl <span class="built_in">enable</span> nginx</span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验1-5】Nginx安装启动成功"><a href="#✅-【成功校验1-5】Nginx安装启动成功" class="headerlink" title="✅ 【成功校验1.5】Nginx安装启动成功"></a>✅ 【成功校验1.5】Nginx安装启动成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status nginx &amp;&amp; curl http://127.0.0.1</span><br></pre></td></tr></table></figure><ul><li>成功标志：状态显示 <code>active (running)</code> + curl返回Nginx默认欢迎页HTML内容</li></ul><hr><h2 id="—-流程二：标准化目录结构搭建（多项目隔离-前后端分离，重中之重，杜绝混乱）"><a href="#—-流程二：标准化目录结构搭建（多项目隔离-前后端分离，重中之重，杜绝混乱）" class="headerlink" title="— 流程二：标准化目录结构搭建（多项目隔离+前后端分离，重中之重，杜绝混乱）"></a>— 流程二：标准化目录结构搭建（多项目隔离+前后端分离，重中之重，杜绝混乱）</h2><h3 id="核心说明"><a href="#核心说明" class="headerlink" title="核心说明"></a>核心说明</h3><ol><li>所有项目统一放在 <code>/opt/</code> 目录下，分为【前端目录】和【后端目录】，彻底分离</li><li>每个SpringBoot项目独立一个文件夹，包含：后端jar包、docker-compose.yml、数据库&#x2F;Redis持久化目录，互不干扰</li><li>所有目录权限全开，避免Docker挂载&#x2F;访问权限问题</li><li>示例：本次搭建2个SpringBoot项目 <code>gy-booking(预约系统)</code> + <code>oa-system(办公系统)</code>，适配「多项目」场景，可无限扩展</li></ol><h3 id="2-1-创建全局统一根目录（所有项目的父目录）"><a href="#2-1-创建全局统一根目录（所有项目的父目录）" class="headerlink" title="2.1 创建全局统一根目录（所有项目的父目录）"></a>2.1 创建全局统一根目录（所有项目的父目录）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 前端静态文件根目录（所有项目的前端打包文件都放这里）</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/frontend &amp;&amp; <span class="built_in">chmod</span> -R 777 /opt/frontend</span><br><span class="line"><span class="comment"># 后端项目根目录（所有项目的后端Docker配置都放这里）</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/backend &amp;&amp; <span class="built_in">chmod</span> -R 777 /opt/backend</span><br></pre></td></tr></table></figure><h3 id="2-2-创建单个项目的标准化目录（示例：gy-booking-预约系统，复制可创建多个项目）"><a href="#2-2-创建单个项目的标准化目录（示例：gy-booking-预约系统，复制可创建多个项目）" class="headerlink" title="2.2 创建单个项目的标准化目录（示例：gy-booking 预约系统，复制可创建多个项目）"></a>2.2 创建单个项目的标准化目录（示例：gy-booking 预约系统，复制可创建多个项目）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># ========== 项目1：gy-booking 预约系统 ==========</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/backend/gy-booking/&#123;app,mysql/conf,mysql/data,mysql/init,redis/conf,redis/data&#125;</span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/frontend/gy-booking</span><br><span class="line"><span class="comment"># ========== 项目2：oa-system 办公系统（多项目扩展示例） ==========</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/backend/oa-system/&#123;app,mysql/conf,mysql/data,mysql/init,redis/conf,redis/data&#125;</span><br><span class="line"><span class="built_in">mkdir</span> -p /opt/frontend/oa-system</span><br></pre></td></tr></table></figure><h3 id="2-3-上传项目文件到对应目录（纯命令行-x2F-ftp工具均可，核心路径对应）"><a href="#2-3-上传项目文件到对应目录（纯命令行-x2F-ftp工具均可，核心路径对应）" class="headerlink" title="2.3 上传项目文件到对应目录（纯命令行&#x2F;ftp工具均可，核心路径对应）"></a>2.3 上传项目文件到对应目录（纯命令行&#x2F;ftp工具均可，核心路径对应）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 后端：将SpringBoot打包好的 app.jar 上传至【对应项目】的app目录</span></span><br><span class="line"><span class="comment"># 例：gy-booking的jar包 → /opt/backend/gy-booking/app/app.jar</span></span><br><span class="line"><span class="comment">#    oa-system的jar包 → /opt/backend/oa-system/app/app.jar</span></span><br><span class="line"><span class="comment"># 2. 前端：将前端打包好的静态文件（Vue/React打包后的dist目录所有文件）上传至前端目录</span></span><br><span class="line"><span class="comment"># 例：gy-booking的前端 → /opt/frontend/gy-booking/</span></span><br><span class="line"><span class="comment">#    oa-system的前端 → /opt/frontend/oa-system/</span></span><br><span class="line"><span class="comment"># 3. 可选：上传mysql初始化sql、redis.conf配置文件到对应目录</span></span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验2-3】文件上传成功"><a href="#✅-【成功校验2-3】文件上传成功" class="headerlink" title="✅ 【成功校验2.3】文件上传成功"></a>✅ 【成功校验2.3】文件上传成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 校验后端jar包是否存在</span></span><br><span class="line"><span class="built_in">ls</span> -l /opt/backend/gy-booking/app/app.jar</span><br><span class="line"><span class="comment"># 校验前端文件是否存在</span></span><br><span class="line"><span class="built_in">ls</span> -l /opt/frontend/gy-booking/index.html</span><br></pre></td></tr></table></figure><ul><li>成功标志：能看到对应文件，无 <code>No such file or directory</code> 报错</li></ul><h3 id="✅-目录结构规范（必看，所有项目通用，记死）"><a href="#✅-目录结构规范（必看，所有项目通用，记死）" class="headerlink" title="✅ 目录结构规范（必看，所有项目通用，记死）"></a>✅ 目录结构规范（必看，所有项目通用，记死）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/opt</span><br><span class="line">├── frontend/          # 所有项目前端静态文件根目录</span><br><span class="line">│   ├── gy-booking/    # gy-booking项目前端（Vue/React打包文件）</span><br><span class="line">│   └── oa-system/     # oa-system项目前端</span><br><span class="line">└── backend/           # 所有项目后端Docker根目录</span><br><span class="line">    ├── gy-booking/    # gy-booking项目后端</span><br><span class="line">    │   ├── app/       # 存放springboot的app.jar</span><br><span class="line">    │   ├── mysql/     # mysql持久化目录（配置/数据/初始化sql）</span><br><span class="line">    │   └── redis/     # redis持久化目录（配置/数据）</span><br><span class="line">    └── oa-system/     # oa-system项目后端（同gy-booking结构）</span><br></pre></td></tr></table></figure><hr><h2 id="—-流程三：Docker容器化部署（SpringBoot-MySQL-Redis，纯命令行，无Portainer，核心流程）"><a href="#—-流程三：Docker容器化部署（SpringBoot-MySQL-Redis，纯命令行，无Portainer，核心流程）" class="headerlink" title="— 流程三：Docker容器化部署（SpringBoot+MySQL+Redis，纯命令行，无Portainer，核心流程）"></a>— 流程三：Docker容器化部署（SpringBoot+MySQL+Redis，纯命令行，无Portainer，核心流程）</h2><h3 id="核心说明-1"><a href="#核心说明-1" class="headerlink" title="核心说明"></a>核心说明</h3><ol><li>每个项目独立编写 <code>docker-compose.yml</code>，<strong>多项目完全隔离</strong>，启停互不影响</li><li>所有容器的端口均映射到 <code>127.0.0.1</code>（本机回环地址），<strong>不暴露外网端口</strong> → 无需防火墙&#x2F;安全组权限，彻底规避端口占用&#x2F;访问限制</li><li>配置MySQL&#x2F;Redis持久化挂载，容器删除后数据不丢失，生产级必备</li><li>SpringBoot项目无需修改任何配置（端口默认8080，上下文路径如&#x2F;gy-booking）</li><li>纯docker-compose命令操作，一键启动&#x2F;停止&#x2F;重启&#x2F;查看日志，无图形化</li></ol><h3 id="3-1-编写项目的docker-compose-yml（纯命令行vim编写，核心配置，复制即用）"><a href="#3-1-编写项目的docker-compose-yml（纯命令行vim编写，核心配置，复制即用）" class="headerlink" title="3.1 编写项目的docker-compose.yml（纯命令行vim编写，核心配置，复制即用）"></a>3.1 编写项目的docker-compose.yml（纯命令行vim编写，核心配置，复制即用）</h3><h4 id="示例：gy-booking项目的docker-compose-yml（路径：-x2F-opt-x2F-backend-x2F-gy-booking-x2F-docker-compose-yml）"><a href="#示例：gy-booking项目的docker-compose-yml（路径：-x2F-opt-x2F-backend-x2F-gy-booking-x2F-docker-compose-yml）" class="headerlink" title="示例：gy-booking项目的docker-compose.yml（路径：&#x2F;opt&#x2F;backend&#x2F;gy-booking&#x2F;docker-compose.yml）"></a>示例：gy-booking项目的docker-compose.yml（路径：&#x2F;opt&#x2F;backend&#x2F;gy-booking&#x2F;docker-compose.yml）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /opt/backend/gy-booking/docker-compose.yml</span><br></pre></td></tr></table></figure><p>粘贴以下配置（<strong>无需修改，直接用，生产级稳定版</strong>，适配所有SpringBoot项目）：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;3.8&#x27;</span></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="comment"># SpringBoot后端服务</span></span><br><span class="line">  <span class="attr">app:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">openjdk:17-jre-slim</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./app:/app</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">MYSQL_HOST=mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">MYSQL_PORT=3306</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">REDIS_HOST=redis</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">REDIS_PORT=6379</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">redis</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">java</span> <span class="string">-jar</span> <span class="string">/app/app.jar</span></span><br><span class="line">    <span class="comment"># 核心：仅本机访问，映射到127.0.0.1，不暴露外网</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;127.0.0.1:8081:8080&quot;</span></span><br><span class="line">    <span class="attr">networks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">gy-network</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># MySQL数据库</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./mysql/conf:/etc/mysql/conf.d</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./mysql/data:/var/lib/mysql</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./mysql/init:/docker-entrypoint-initdb.d</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">MYSQL_ROOT_PASSWORD=123456</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">MYSQL_DATABASE=gy_booking</span></span><br><span class="line">    <span class="attr">networks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">gy-network</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># Redis缓存</span></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">redis:7.0</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./redis/conf/redis.conf:/etc/redis/redis.conf</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">./redis/data:/data</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">redis-server</span> <span class="string">/etc/redis/redis.conf</span></span><br><span class="line">    <span class="attr">networks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">gy-network</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 独立网络，项目间隔离，避免容器通信冲突</span></span><br><span class="line"><span class="attr">networks:</span></span><br><span class="line">  <span class="attr">gy-network:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">bridge</span></span><br></pre></td></tr></table></figure><h3 id="3-2-启动项目容器（纯命令行，一键启动，核心命令）"><a href="#3-2-启动项目容器（纯命令行，一键启动，核心命令）" class="headerlink" title="3.2 启动项目容器（纯命令行，一键启动，核心命令）"></a>3.2 启动项目容器（纯命令行，一键启动，核心命令）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入项目目录</span></span><br><span class="line"><span class="built_in">cd</span> /opt/backend/gy-booking</span><br><span class="line"><span class="comment"># 构建+启动容器（后台运行，首次启动加--build，后续无需）</span></span><br><span class="line">docker-compose up -d --build</span><br></pre></td></tr></table></figure><h3 id="3-3-多项目扩展：重复3-1-3-2步骤，编写其他项目的docker-compose-yml并启动即可，无冲突"><a href="#3-3-多项目扩展：重复3-1-3-2步骤，编写其他项目的docker-compose-yml并启动即可，无冲突" class="headerlink" title="3.3 多项目扩展：重复3.1-3.2步骤，编写其他项目的docker-compose.yml并启动即可，无冲突"></a>3.3 多项目扩展：重复3.1-3.2步骤，编写其他项目的docker-compose.yml并启动即可，无冲突</h3><h3 id="✅-容器相关核心命令（纯命令行，必备，所有项目通用）"><a href="#✅-容器相关核心命令（纯命令行，必备，所有项目通用）" class="headerlink" title="✅ 容器相关核心命令（纯命令行，必备，所有项目通用）"></a>✅ 容器相关核心命令（纯命令行，必备，所有项目通用）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 查看当前项目容器运行状态</span></span><br><span class="line">docker-compose ps</span><br><span class="line"><span class="comment"># 2. 查看SpringBoot启动日志（排查项目启动报错）</span></span><br><span class="line">docker-compose logs -f app</span><br><span class="line"><span class="comment"># 3. 重启当前项目所有容器</span></span><br><span class="line">docker-compose restart</span><br><span class="line"><span class="comment"># 4. 停止当前项目所有容器（不删除数据）</span></span><br><span class="line">docker-compose stop</span><br><span class="line"><span class="comment"># 5. 停止并删除当前项目所有容器（数据持久化目录不会删除）</span></span><br><span class="line">docker-compose down</span><br><span class="line"><span class="comment"># 6. 查看服务器所有容器状态</span></span><br><span class="line">docker ps -a</span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验3-2】容器部署启动成功"><a href="#✅-【成功校验3-2】容器部署启动成功" class="headerlink" title="✅ 【成功校验3.2】容器部署启动成功"></a>✅ 【成功校验3.2】容器部署启动成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 校验容器状态</span></span><br><span class="line">docker-compose ps</span><br><span class="line"><span class="comment"># 本机访问SpringBoot接口，验证是否启动成功</span></span><br><span class="line">curl http://127.0.0.1:8081/gy-booking/role/getList</span><br></pre></td></tr></table></figure><ul><li>成功标志：<ol><li><code>docker-compose ps</code> 显示所有容器状态为 <code>Up</code></li><li>curl返回接口JSON数据（无超时&#x2F;拒绝），无报错</li></ol></li><li>核心关键：<strong>本机能访问，外网访问不到</strong> → 正常，后续由Nginx转发</li></ul><hr><h2 id="—-流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目隔离，域名访问核心，无任何权限要求）"><a href="#—-流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目隔离，域名访问核心，无任何权限要求）" class="headerlink" title="— 流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目隔离，域名访问核心，无任何权限要求）"></a>— 流程四：本地Nginx反向代理核心配置（80端口唯一入口，多项目隔离，域名访问核心，无任何权限要求）</h2><h3 id="核心说明【重中之重，解决所有访问问题】"><a href="#核心说明【重中之重，解决所有访问问题】" class="headerlink" title="核心说明【重中之重，解决所有访问问题】"></a>核心说明【重中之重，解决所有访问问题】</h3><ol><li>服务器<strong>仅开放80端口</strong>，所有外部请求通过80端口进入，由Nginx统一转发，无需防火墙&#x2F;安全组权限</li><li><strong>核心价值</strong>：Nginx通过「路径前缀」实现多项目彻底隔离，不同项目对应不同路径，互不干扰，完美解决原有项目跳转问题</li><li><strong>前后端分离转发规则</strong>：<ul><li>前端：Nginx直接读取本地静态文件，速度最快</li><li>后端：Nginx将请求<strong>本机内部转发</strong>到SpringBoot容器的本机端口，无外网暴露，无超时</li></ul></li><li>配置中加入「防跳转&#x2F;防污染」指令，彻底屏蔽服务器原有项目的跳转规则，你的项目绝对不会被强制跳转登录页</li><li>所有配置为<strong>生产级最优配置</strong>，包含跨域、IP透传、Cookie&#x2F;Session保持、静态文件缓存，缺一不可</li></ol><h3 id="4-1-Nginx核心规则：不修改原有配置，新增独立配置文件（最佳实践，不影响其他项目）"><a href="#4-1-Nginx核心规则：不修改原有配置，新增独立配置文件（最佳实践，不影响其他项目）" class="headerlink" title="4.1 Nginx核心规则：不修改原有配置，新增独立配置文件（最佳实践，不影响其他项目）"></a>4.1 Nginx核心规则：不修改原有配置，新增独立配置文件（最佳实践，不影响其他项目）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 进入Nginx自定义配置目录（Ubuntu固定路径）</span></span><br><span class="line"><span class="built_in">cd</span> /etc/nginx/conf.d</span><br><span class="line"><span class="comment"># 创建多项目转发配置文件（核心配置文件，所有项目的转发规则都写这里）</span></span><br><span class="line">vim all-projects.conf</span><br></pre></td></tr></table></figure><h3 id="4-2-粘贴【完整无错的Nginx多项目转发配置】（直接复制，一行不改，生产级稳定版，前后端分离-多项目隔离）"><a href="#4-2-粘贴【完整无错的Nginx多项目转发配置】（直接复制，一行不改，生产级稳定版，前后端分离-多项目隔离）" class="headerlink" title="4.2 粘贴【完整无错的Nginx多项目转发配置】（直接复制，一行不改，生产级稳定版，前后端分离+多项目隔离）"></a>4.2 粘贴【完整无错的Nginx多项目转发配置】（直接复制，一行不改，生产级稳定版，前后端分离+多项目隔离）</h3><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 服务器唯一入口：80端口 + 公网IP + 域名，所有项目共用一个server即可</span></span><br><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span> <span class="number">80</span>;</span><br><span class="line">    <span class="attribute">server_name</span> <span class="number">106.15.90.217</span> synyingx.cn; <span class="comment"># 你的服务器公网IP + 绑定的域名</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># ===================== 项目1：gy-booking 预约系统 【前后端分离完整配置】 =====================</span></span><br><span class="line">    <span class="comment"># ★ 后端接口转发：外部访问 → 域名/gy-api/xxx → 转发到本机8081端口的SpringBoot后端</span></span><br><span class="line">    <span class="section">location</span> /gy-api/ &#123;</span><br><span class="line">        <span class="comment"># 核心防跳转：终止所有后续跳转规则，彻底屏蔽原有项目的强制跳转，解决你的核心痛点</span></span><br><span class="line">        <span class="attribute">rewrite</span><span class="regexp"> ^(.*)$</span> <span class="variable">$1</span> <span class="literal">break</span>;</span><br><span class="line">        <span class="attribute">proxy_redirect</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="attribute">proxy_cookie_path</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="comment"># 转发到gy-booking的SpringBoot容器本机地址</span></span><br><span class="line">        <span class="attribute">proxy_pass</span> http://127.0.0.1:8081/gy-booking/;</span><br><span class="line">        <span class="comment"># 必备请求头：解决跨域、IP获取、Cookie/Session失效、接口报错，缺一不可</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Forwarded-Proto <span class="variable">$scheme</span>;</span><br><span class="line">        <span class="attribute">proxy_http_version</span> <span class="number">1</span>.<span class="number">1</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> Connection <span class="string">&quot;&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment"># ★ 前端页面转发：外部访问 → 域名/gy-web/ → 直接读取本地前端静态文件，前后端分离</span></span><br><span class="line">    <span class="section">location</span> /gy-web/ &#123;</span><br><span class="line">        <span class="attribute">root</span> /opt/frontend;</span><br><span class="line">        <span class="attribute">index</span> index.html index.htm;</span><br><span class="line">        <span class="comment"># Vue/React history路由模式必备，解决刷新404问题</span></span><br><span class="line">        <span class="attribute">try_files</span> <span class="variable">$uri</span> <span class="variable">$uri</span>/ /gy-web/index.html;</span><br><span class="line">        <span class="comment"># 防跳转+静态文件缓存，提升访问速度</span></span><br><span class="line">        <span class="attribute">rewrite</span><span class="regexp"> ^(.*)$</span> <span class="variable">$1</span> <span class="literal">break</span>;</span><br><span class="line">        <span class="attribute">proxy_redirect</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="attribute">expires</span> <span class="number">30d</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># ===================== 项目2：oa-system 办公系统 【多项目扩展示例，复制即可新增】 =====================</span></span><br><span class="line">    <span class="section">location</span> /oa-api/ &#123;</span><br><span class="line">        <span class="attribute">rewrite</span><span class="regexp"> ^(.*)$</span> <span class="variable">$1</span> <span class="literal">break</span>;</span><br><span class="line">        <span class="attribute">proxy_redirect</span> <span class="literal">off</span>;</span><br><span class="line">        <span class="attribute">proxy_pass</span> http://127.0.0.1:8082/oa-system/; <span class="comment"># 对应oa-system的本机端口</span></span><br><span class="line">        <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">        <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="section">location</span> /oa-web/ &#123;</span><br><span class="line">        <span class="attribute">root</span> /opt/frontend;</span><br><span class="line">        <span class="attribute">try_files</span> <span class="variable">$uri</span> <span class="variable">$uri</span>/ /oa-web/index.html;</span><br><span class="line">        <span class="attribute">rewrite</span><span class="regexp"> ^(.*)$</span> <span class="variable">$1</span> <span class="literal">break</span>;</span><br><span class="line">        <span class="attribute">proxy_redirect</span> <span class="literal">off</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># ===================== 原有项目规则：完全保留，无任何修改 =====================</span></span><br><span class="line">    <span class="comment"># 原有项目的请求会自动匹配原有配置，和你的新项目完全隔离，互不影响</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-3-校验Nginx配置语法-平滑重启Nginx（核心步骤，配置生效，不影响其他项目）"><a href="#4-3-校验Nginx配置语法-平滑重启Nginx（核心步骤，配置生效，不影响其他项目）" class="headerlink" title="4.3 校验Nginx配置语法+平滑重启Nginx（核心步骤，配置生效，不影响其他项目）"></a>4.3 校验Nginx配置语法+平滑重启Nginx（核心步骤，配置生效，不影响其他项目）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 校验配置语法是否正确（必做，有错误会精准提示，无错误则显示success）</span></span><br><span class="line">nginx -t</span><br><span class="line"><span class="comment"># 2. 平滑重启Nginx，配置生效（关键：不中断现有服务，原有项目无感知）</span></span><br><span class="line">systemctl reload nginx</span><br></pre></td></tr></table></figure><h4 id="✅-【成功校验4-3】Nginx配置生效成功"><a href="#✅-【成功校验4-3】Nginx配置生效成功" class="headerlink" title="✅ 【成功校验4.3】Nginx配置生效成功"></a>✅ 【成功校验4.3】Nginx配置生效成功</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status nginx</span><br></pre></td></tr></table></figure><ul><li>成功标志：Nginx状态显示 <code>active (running)</code>，无报错</li></ul><h3 id="✅-Nginx核心规则总结（终身受用，多项目隔离的底层逻辑）"><a href="#✅-Nginx核心规则总结（终身受用，多项目隔离的底层逻辑）" class="headerlink" title="✅ Nginx核心规则总结（终身受用，多项目隔离的底层逻辑）"></a>✅ Nginx核心规则总结（终身受用，多项目隔离的底层逻辑）</h3><ol><li><strong>精准前缀匹配</strong>：<code>location /前缀/</code> 只匹配以该前缀开头的请求，不同项目不同前缀，绝对无冲突</li><li><strong>独立规则隔离</strong>：每个项目的前端&#x2F;后端规则都是独立的，A项目的配置不会影响B项目</li><li><strong>本机内部转发</strong>：Nginx转发是服务器本机通信，不走外网，无需防火墙放行，速度极快，永不超时</li></ol><hr><h2 id="—-流程五：域名解析-最终访问校验-运维常用命令（收尾，生产级维护，一键搞定）"><a href="#—-流程五：域名解析-最终访问校验-运维常用命令（收尾，生产级维护，一键搞定）" class="headerlink" title="— 流程五：域名解析+最终访问校验+运维常用命令（收尾，生产级维护，一键搞定）"></a>— 流程五：域名解析+最终访问校验+运维常用命令（收尾，生产级维护，一键搞定）</h2><h3 id="5-1-域名解析配置（无需服务器操作，域名控制台配置，1分钟搞定）"><a href="#5-1-域名解析配置（无需服务器操作，域名控制台配置，1分钟搞定）" class="headerlink" title="5.1 域名解析配置（无需服务器操作，域名控制台配置，1分钟搞定）"></a>5.1 域名解析配置（无需服务器操作，域名控制台配置，1分钟搞定）</h3><ul><li>操作平台：你的域名服务商控制台（阿里云&#x2F;腾讯云&#x2F;华为云）</li><li>配置规则：添加一条【A记录】，将你的域名 <code>synyingx.cn</code> 解析到服务器公网IP <code>106.15.90.217</code></li><li>生效时间：配置后1-5分钟生效，无需重启服务器&#x2F;容器&#x2F;Nginx</li></ul><h3 id="5-2-最终访问规则（核心！所有项目通用，无端口、无跳转、直接访问，域名访问成功的标准）"><a href="#5-2-最终访问规则（核心！所有项目通用，无端口、无跳转、直接访问，域名访问成功的标准）" class="headerlink" title="5.2 最终访问规则（核心！所有项目通用，无端口、无跳转、直接访问，域名访问成功的标准）"></a>5.2 最终访问规则（核心！所有项目通用，无端口、无跳转、直接访问，域名访问成功的标准）</h3><blockquote><p>✅ 核心口诀：<strong>前端走 &#x2F;项目-web 后缀，后端走 &#x2F;项目-api 后缀，无端口号，直接域名访问</strong></p></blockquote><h4 id="示例：gy-booking-预约系统（前后端分离）"><a href="#示例：gy-booking-预约系统（前后端分离）" class="headerlink" title="示例：gy-booking 预约系统（前后端分离）"></a>示例：gy-booking 预约系统（前后端分离）</h4><ol><li>前端页面访问地址：<code>http://synyingx.cn/gy-web/</code></li><li>后端接口访问地址：<code>http://synyingx.cn/gy-api/role/getList</code></li></ol><h4 id="示例：oa-system-办公系统（多项目扩展）"><a href="#示例：oa-system-办公系统（多项目扩展）" class="headerlink" title="示例：oa-system 办公系统（多项目扩展）"></a>示例：oa-system 办公系统（多项目扩展）</h4><ol><li>前端页面访问地址：<code>http://synyingx.cn/oa-web/</code></li><li>后端接口访问地址：<code>http://synyingx.cn/oa-api/user/getInfo</code></li></ol><h3 id="✅-【终极成功校验】纯命令行-浏览器双校验（100-确认所有功能正常）"><a href="#✅-【终极成功校验】纯命令行-浏览器双校验（100-确认所有功能正常）" class="headerlink" title="✅ 【终极成功校验】纯命令行+浏览器双校验（100%确认所有功能正常）"></a>✅ 【终极成功校验】纯命令行+浏览器双校验（100%确认所有功能正常）</h3><h4 id="校验1：纯命令行校验（服务器本地访问，最精准）"><a href="#校验1：纯命令行校验（服务器本地访问，最精准）" class="headerlink" title="校验1：纯命令行校验（服务器本地访问，最精准）"></a>校验1：纯命令行校验（服务器本地访问，最精准）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 校验后端接口是否正常返回数据</span></span><br><span class="line">curl -X GET http://synyingx.cn/gy-api/role/getList</span><br><span class="line"><span class="comment"># 校验前端页面是否正常返回</span></span><br><span class="line">curl http://synyingx.cn/gy-web/index.html</span><br></pre></td></tr></table></figure><ul><li>成功标志：接口返回JSON业务数据，前端返回HTML代码，无超时&#x2F;404&#x2F;跳转</li></ul><h4 id="校验2：浏览器校验（外网访问，最终形态）"><a href="#校验2：浏览器校验（外网访问，最终形态）" class="headerlink" title="校验2：浏览器校验（外网访问，最终形态）"></a>校验2：浏览器校验（外网访问，最终形态）</h4><ul><li>打开浏览器，输入上述访问地址，能正常加载前端页面、调用后端接口、查询数据库数据，<strong>无跳转登录页、无超时、无跨域报错</strong> → 部署成功！</li></ul><h3 id="5-3-生产级运维常用命令（纯命令行，所有操作一键搞定，无图形化，必备收藏）"><a href="#5-3-生产级运维常用命令（纯命令行，所有操作一键搞定，无图形化，必备收藏）" class="headerlink" title="5.3 生产级运维常用命令（纯命令行，所有操作一键搞定，无图形化，必备收藏）"></a>5.3 生产级运维常用命令（纯命令行，所有操作一键搞定，无图形化，必备收藏）</h3><h4 id="✅-一、Docker容器相关（项目启停-x2F-日志-x2F-更新，所有项目通用）"><a href="#✅-一、Docker容器相关（项目启停-x2F-日志-x2F-更新，所有项目通用）" class="headerlink" title="✅ 一、Docker容器相关（项目启停&#x2F;日志&#x2F;更新，所有项目通用）"></a>✅ 一、Docker容器相关（项目启停&#x2F;日志&#x2F;更新，所有项目通用）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 进入指定项目目录（如gy-booking）</span></span><br><span class="line"><span class="built_in">cd</span> /opt/backend/gy-booking</span><br><span class="line"><span class="comment"># 2. 查看项目启动日志（排查接口报错）</span></span><br><span class="line">docker-compose logs -f app</span><br><span class="line"><span class="comment"># 3. 重启项目容器（更新jar包后必用）</span></span><br><span class="line">docker-compose restart app</span><br><span class="line"><span class="comment"># 4. 停止项目容器（维护时用）</span></span><br><span class="line">docker-compose stop</span><br><span class="line"><span class="comment"># 5. 彻底重建容器（配置修改后必用）</span></span><br><span class="line">docker-compose down &amp;&amp; docker-compose up -d --build</span><br></pre></td></tr></table></figure><h4 id="✅-二、Nginx相关（配置修改-x2F-重启-x2F-状态）"><a href="#✅-二、Nginx相关（配置修改-x2F-重启-x2F-状态）" class="headerlink" title="✅ 二、Nginx相关（配置修改&#x2F;重启&#x2F;状态）"></a>✅ 二、Nginx相关（配置修改&#x2F;重启&#x2F;状态）</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 校验Nginx配置语法</span></span><br><span class="line">nginx -t</span><br><span class="line"><span class="comment"># 2. 平滑重启Nginx（配置修改后生效）</span></span><br><span class="line">systemctl reload nginx</span><br><span class="line"><span class="comment"># 3. 查看Nginx运行状态</span></span><br><span class="line">systemctl status nginx</span><br><span class="line"><span class="comment"># 4. 查看Nginx访问日志（排查转发问题）</span></span><br><span class="line"><span class="built_in">tail</span> -f /var/log/nginx/access.log</span><br></pre></td></tr></table></figure><h4 id="✅-三、项目更新最简流程（前后端通用，1分钟搞定，无需复杂操作）"><a href="#✅-三、项目更新最简流程（前后端通用，1分钟搞定，无需复杂操作）" class="headerlink" title="✅ 三、项目更新最简流程（前后端通用，1分钟搞定，无需复杂操作）"></a>✅ 三、项目更新最简流程（前后端通用，1分钟搞定，无需复杂操作）</h4><h5 id="✔️-后端更新（SpringBoot）"><a href="#✔️-后端更新（SpringBoot）" class="headerlink" title="✔️ 后端更新（SpringBoot）"></a>✔️ 后端更新（SpringBoot）</h5><ol><li>本地打包新的 <code>app.jar</code> → 上传覆盖 <code>/opt/backend/项目名/app/app.jar</code></li><li>执行命令：<code>cd /opt/backend/项目名 &amp;&amp; docker-compose restart app</code> → 生效</li></ol><h5 id="✔️-前端更新（Vue-x2F-React）"><a href="#✔️-前端更新（Vue-x2F-React）" class="headerlink" title="✔️ 前端更新（Vue&#x2F;React）"></a>✔️ 前端更新（Vue&#x2F;React）</h5><ol><li>本地打包新的前端静态文件 → 上传覆盖 <code>/opt/frontend/项目名/</code> 下所有文件</li><li>执行命令：<code>systemctl reload nginx</code> → 生效（无需重启容器）</li></ol><hr><h2 id="✅-补充：多项目扩展极简方法（未来新增项目，仅需3步，1分钟搞定）"><a href="#✅-补充：多项目扩展极简方法（未来新增项目，仅需3步，1分钟搞定）" class="headerlink" title="✅ 补充：多项目扩展极简方法（未来新增项目，仅需3步，1分钟搞定）"></a>✅ 补充：多项目扩展极简方法（未来新增项目，仅需3步，1分钟搞定）</h2><ol><li>按流程二创建新项目的目录结构 → <code>/opt/backend/新项目名</code> + <code>/opt/frontend/新项目名</code></li><li>按流程三编写docker-compose.yml，启动容器（映射本机新端口如8083）</li><li>按流程四在Nginx的<code>all-projects.conf</code>中新增2个location规则（&#x2F;新项目-api&#x2F; + &#x2F;新项目-web&#x2F;），重启Nginx即可</li></ol><hr><h2 id="✅-最终完美架构总结（无任何坑，生产级稳定，你的最终形态）"><a href="#✅-最终完美架构总结（无任何坑，生产级稳定，你的最终形态）" class="headerlink" title="✅ 最终完美架构总结（无任何坑，生产级稳定，你的最终形态）"></a>✅ 最终完美架构总结（无任何坑，生产级稳定，你的最终形态）</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">外网浏览器 → 域名(synyingx.cn) → 服务器80端口 → 本地Nginx反向代理</span><br><span class="line">  ├─ 前端请求 → Nginx直接读取 /opt/frontend/项目名 静态文件 → 返回页面</span><br><span class="line">  └─ 后端请求 → Nginx本机转发 → 127.0.0.1:端口 → SpringBoot容器 → 返回接口数据</span><br><span class="line">Docker容器：SpringBoot+MySQL+Redis 本机运行，无外网暴露，安全稳定</span><br></pre></td></tr></table></figure><h3 id="✅-架构核心优势（完美适配你的所有限制）"><a href="#✅-架构核心优势（完美适配你的所有限制）" class="headerlink" title="✅ 架构核心优势（完美适配你的所有限制）"></a>✅ 架构核心优势（完美适配你的所有限制）</h3><ol><li>✔️ 无防火墙&#x2F;安全组权限：仅用80端口，无需开任何新端口</li><li>✔️ 多项目隔离：不同项目不同路径，互不干扰，无冲突</li><li>✔️ 前后端分离：前端静态文件独立部署，后端容器化运行，维护方便</li><li>✔️ 纯命令行操作：无图形化，所有操作可自动化，适合生产环境</li><li>✔️ 数据持久化：MySQL&#x2F;Redis数据不丢失，项目更新无风险</li></ol><hr><h2 id="✅-常见问题排查（纯命令行，精准定位，无需额外工具）"><a href="#✅-常见问题排查（纯命令行，精准定位，无需额外工具）" class="headerlink" title="✅ 常见问题排查（纯命令行，精准定位，无需额外工具）"></a>✅ 常见问题排查（纯命令行，精准定位，无需额外工具）</h2><ol><li><strong>接口访问超时</strong>：检查容器是否Up → <code>docker-compose ps</code>，检查本机是否能访问 → <code>curl 127.0.0.1:8081/xxx</code></li><li><strong>前端404</strong>：检查前端文件路径是否正确 → <code>ls /opt/frontend/项目名/index.html</code>，检查Nginx的root路径是否正确</li><li><strong>接口404</strong>：检查SpringBoot上下文路径是否和Nginx的proxy_pass一致，检查接口路径拼写</li><li><strong>被强制跳转登录页</strong>：确认Nginx配置中加了 <code>rewrite ^(.*)$ $1 break; proxy_redirect off;</code>，重启Nginx即可</li></ol><hr><p>🎉 至此，你已完成「从Docker安装到域名访问多项目」的全流程纯命令行部署，所有步骤均为生产级标准，无任何冗余，项目稳定运行，后续维护极简！恭喜你掌握了云服务器多项目部署的核心精髓！ 🎉</p>]]></content>
      
      
      
        <tags>
            
            <tag> docker </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>自动内存管理</title>
      <link href="/2025/12/26/Automatic%20memory%20management/"/>
      <url>/2025/12/26/Automatic%20memory%20management/</url>
      
        <content type="html"><![CDATA[<h1 id="《深入理解Java虚拟机》第三版"><a href="#《深入理解Java虚拟机》第三版" class="headerlink" title="《深入理解Java虚拟机》第三版"></a>《深入理解Java虚拟机》第三版</h1><h2 id="第二章-自动内存管理-——-知识笔记"><a href="#第二章-自动内存管理-——-知识笔记" class="headerlink" title="第二章 自动内存管理 —— 知识笔记"></a>第二章 自动内存管理 —— 知识笔记</h2><hr><h3 id="一、JVM运行时数据区域划分"><a href="#一、JVM运行时数据区域划分" class="headerlink" title="一、JVM运行时数据区域划分"></a>一、JVM运行时数据区域划分</h3><p>JVM在执行Java程序时，会将内存划分为若干个运行时数据区。根据《Java虚拟机规范》，主要分为以下 <strong>5 + 1</strong> 个区域：</p><table><thead><tr><th>区域</th><th>线程共享&#x2F;私有</th><th>功能说明</th><th>是否可能 OOM</th></tr></thead><tbody><tr><td><strong>程序计数器</strong></td><td>私有</td><td>记录当前线程执行的字节码行号（本地方法时为 undefined）</td><td>❌ 不会OOM</td></tr><tr><td><strong>Java虚拟机栈</strong></td><td>私有</td><td>存储栈帧（局部变量表、操作数栈、动态链接、返回地址）</td><td>✅ StackOverflowError &#x2F; OutOfMemoryError</td></tr><tr><td><strong>本地方法栈</strong></td><td>私有</td><td>为 Native 方法服务（如 C&#x2F;C++）</td><td>✅ 同上</td></tr><tr><td><strong>Java堆（Heap）</strong></td><td>共享</td><td>几乎所有对象实例和数组分配于此；GC 主战场</td><td>✅ <code>OutOfMemoryError: Java heap space</code></td></tr><tr><td><strong>方法区（Method Area）</strong></td><td>共享</td><td>存储类元信息、常量、静态变量、JIT 编译代码</td><td>✅ JDK8+ 为 <code>Metaspace OOM</code></td></tr><tr><td><strong>直接内存（Direct Memory）</strong></td><td>非JVM区域</td><td>通过 <code>ByteBuffer.allocateDirect()</code> 分配堆外内存</td><td>✅ 受 <code>-XX:MaxDirectMemorySize</code> 限制</td></tr></tbody></table><hr><h3 id="二、各区域详解"><a href="#二、各区域详解" class="headerlink" title="二、各区域详解"></a>二、各区域详解</h3><h4 id="1-程序计数器（Program-Counter-Register）"><a href="#1-程序计数器（Program-Counter-Register）" class="headerlink" title="1. 程序计数器（Program Counter Register）"></a>1. 程序计数器（Program Counter Register）</h4><ul><li><strong>作用</strong>：线程切换后恢复执行位置。</li><li><strong>特点</strong>：线程私有、无 GC、无 OOM。</li></ul><h4 id="2-Java-虚拟机栈"><a href="#2-Java-虚拟机栈" class="headerlink" title="2. Java 虚拟机栈"></a>2. Java 虚拟机栈</h4><ul><li>每个方法调用对应一个 <strong>栈帧（Frame）</strong> 入栈。</li><li><strong>栈帧结构</strong>：<ul><li>局部变量表（Slot 存储基本类型、引用）</li><li>操作数栈</li><li>动态链接（指向运行时常量池）</li><li>方法返回地址</li></ul></li><li><strong>异常</strong>：<ul><li><code>StackOverflowError</code>：递归过深</li><li><code>OutOfMemoryError</code>：线程过多导致栈总内存超限</li></ul></li><li><strong>调优参数</strong>：<code>-Xss1m</code>（默认通常为 1MB）</li></ul><h4 id="3-本地方法栈"><a href="#3-本地方法栈" class="headerlink" title="3. 本地方法栈"></a>3. 本地方法栈</h4><ul><li>与虚拟机栈类似，但服务于 Native 方法。</li><li>HotSpot 中常与 Java 栈合并实现。</li></ul><h4 id="4-Java-堆（重点！）"><a href="#4-Java-堆（重点！）" class="headerlink" title="4. Java 堆（重点！）"></a>4. Java 堆（重点！）</h4><ul><li><strong>分代设计</strong>：<ul><li><strong>新生代（Young Gen）</strong>：Eden + Survivor0 + Survivor1（默认 8:1:1）<ul><li>Minor GC 触发条件：Eden 区满</li></ul></li><li><strong>老年代（Old Gen）</strong><ul><li>Major GC &#x2F; Full GC 触发：老年代空间不足</li></ul></li></ul></li><li><strong>对象分配策略</strong>：<ul><li>优先 Eden</li><li>大对象直接进老年代（避免复制开销）</li><li>动态年龄判定：Survivor 中相同年龄对象 &gt; 50% → 晋升老年代</li></ul></li><li><strong>OOM 场景</strong>：内存泄漏 or 堆太小</li><li><strong>调优参数</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-Xms4g -Xmx4g          <span class="comment"># 固定堆大小</span></span><br><span class="line">-XX:NewRatio=2         <span class="comment"># 老年代:新生代 = 2:1</span></span><br></pre></td></tr></table></figure></li></ul><h4 id="5-方法区（JDK8-为-Metaspace）"><a href="#5-方法区（JDK8-为-Metaspace）" class="headerlink" title="5. 方法区（JDK8+ 为 Metaspace）"></a>5. 方法区（JDK8+ 为 Metaspace）</h4><ul><li>存储：类结构、字段&#x2F;方法数据、静态变量、运行时常量池、JIT 代码</li><li><strong>演进</strong>：<ul><li>JDK7-：永久代（PermGen）→ 易 OOM</li><li>JDK8+：元空间（Metaspace）→ 使用本地内存</li></ul></li><li><strong>OOM 原因</strong>：动态生成大量类（如 CGLib、Spring AOP）</li><li><strong>调优参数</strong>：<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-XX:MetaspaceSize=256m</span><br><span class="line">-XX:MaxMetaspaceSize=512m  <span class="comment"># 默认无上限</span></span><br></pre></td></tr></table></figure></li></ul><h4 id="6-运行时常量池（Runtime-Constant-Pool）"><a href="#6-运行时常量池（Runtime-Constant-Pool）" class="headerlink" title="6. 运行时常量池（Runtime Constant Pool）"></a>6. 运行时常量池（Runtime Constant Pool）</h4><ul><li>方法区的一部分</li><li>存放编译期生成的字面量（如 <code>&quot;hello&quot;</code>）和符号引用</li><li>**JDK7+**：字符串常量池移至 <strong>堆中</strong></li></ul><h4 id="7-直接内存"><a href="#7-直接内存" class="headerlink" title="7. 直接内存"></a>7. 直接内存</h4><ul><li>非 JVM 管理，但常被使用（NIO）</li><li>通过 <code>Unsafe.allocateMemory()</code> 分配</li><li><strong>OOM</strong>：超过 <code>-XX:MaxDirectMemorySize</code>（默认等于 <code>-Xmx</code>）</li></ul><hr><h3 id="三、对象的创建过程（new-指令）"><a href="#三、对象的创建过程（new-指令）" class="headerlink" title="三、对象的创建过程（new 指令）"></a>三、对象的创建过程（<code>new</code> 指令）</h3><ol><li><p><strong>类加载检查</strong><br>→ 检查类是否已加载、解析、初始化（若未，则触发类加载）</p></li><li><p><strong>分配内存</strong>  </p><ul><li><strong>指针碰撞</strong>（Bump the Pointer）：堆规整（Serial、ParNew）</li><li><strong>空闲列表</strong>（Free List）：堆不规整（CMS）</li><li><strong>线程安全</strong>：TLAB（Thread Local Allocation Buffer）或 CAS + 重试</li></ul></li><li><p><strong>初始化零值</strong><br>→ 所有字段设为默认值（int&#x3D;0, boolean&#x3D;false 等）</p></li><li><p><strong>设置对象头</strong>  </p><ul><li>对象所属类（Class 指针）</li><li>哈希码（lazy compute）</li><li>GC 分代年龄</li><li>锁状态（偏向锁、轻量级锁等）</li></ul></li><li><p><strong>执行 <code>&lt;init&gt;</code> 构造方法</strong><br>→ Java 层面的对象初始化完成</p></li></ol><hr><h3 id="四、对象的内存布局"><a href="#四、对象的内存布局" class="headerlink" title="四、对象的内存布局"></a>四、对象的内存布局</h3><table><thead><tr><th>组成</th><th>内容</th></tr></thead><tbody><tr><td><strong>对象头（Header）</strong></td><td>Mark Word（哈希、GC年龄、锁标志）、Class 指针</td></tr><tr><td><strong>实例数据（Instance Data）</strong></td><td>用户定义的字段（按 JVM 字段重排序优化）</td></tr><tr><td><strong>对齐填充（Padding）</strong></td><td>补齐至 8 字节倍数（HotSpot 要求）</td></tr></tbody></table><hr><h3 id="五、对象的访问定位"><a href="#五、对象的访问定位" class="headerlink" title="五、对象的访问定位"></a>五、对象的访问定位</h3><p>JVM 通过 <strong>reference</strong>（引用）访问对象，有两种方式：</p><table><thead><tr><th>方式</th><th>描述</th><th>优缺点</th></tr></thead><tbody><tr><td><strong>句柄访问</strong></td><td>reference → 句柄池 → [对象地址 + 类型信息]</td><td>优点：GC 移动对象只需改句柄；缺点：两次寻址</td></tr><tr><td><strong>直接指针</strong>（主流）</td><td>reference → 对象地址（对象头含 Class 指针）</td><td>优点：速度快；缺点：GC 需更新所有引用</td></tr></tbody></table><blockquote><p>HotSpot 使用 <strong>直接指针</strong>。</p></blockquote><hr><h3 id="六、内存溢出（OOM）实战与排查"><a href="#六、内存溢出（OOM）实战与排查" class="headerlink" title="六、内存溢出（OOM）实战与排查"></a>六、内存溢出（OOM）实战与排查</h3><table><thead><tr><th>OOM 类型</th><th>原因</th><th>排查工具</th></tr></thead><tbody><tr><td><code>Java heap space</code></td><td>对象太多 or 内存泄漏</td><td>MAT、JProfiler、<code>jmap -dump</code></td></tr><tr><td><code>Metaspace</code></td><td>动态类加载过多</td><td><code>jstat -class</code>、检查代理&#x2F;反射</td></tr><tr><td><code>Unable to create new native thread</code></td><td>线程数超系统限制</td><td><code>jstack</code>、<code>ulimit -u</code></td></tr><tr><td><code>Direct buffer memory</code></td><td>NIO 直接内存超限</td><td>检查 <code>ByteBuffer.allocateDirect()</code> 使用</td></tr></tbody></table><p><strong>模拟堆 OOM 示例</strong>：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">HeapOOM</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        List&lt;<span class="type">byte</span>[]&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">            list.add(<span class="keyword">new</span> <span class="title class_">byte</span>[<span class="number">1024</span> * <span class="number">1024</span>]); <span class="comment">// 每次 1MB</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>启动参数：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">-Xmx20m -XX:+HeapDumpOnOutOfMemoryError</span><br></pre></td></tr></table></figure><p>→ 生成 <code>java_pid.hprof</code>，用 <strong>MAT</strong> 分析 GC Roots 引用链。</p><hr><h3 id="七、高频面试题速记"><a href="#七、高频面试题速记" class="headerlink" title="七、高频面试题速记"></a>七、高频面试题速记</h3><ol><li><p><strong>哪些区域线程私有？哪些共享？</strong>  </p><ul><li>私有：程序计数器、虚拟机栈、本地方法栈  </li><li>共享：堆、方法区</li></ul></li><li><p><strong>StackOverflowError 和 OutOfMemoryError 的区别？</strong>  </p><ul><li>SOF：栈深度超限（如无限递归）  </li><li>OOM：无法申请新内存（堆、栈扩展失败、元空间满等）</li></ul></li><li><p><strong>方法区在 JDK8 之后叫什么？</strong><br>→ <strong>元空间（Metaspace）</strong>，使用本地内存</p></li><li><p><strong>对象一定分配在堆上吗？</strong><br>→ 不一定！JVM 可能通过 <strong>逃逸分析 + 标量替换</strong> 将对象分配在栈上（栈上分配）</p></li></ol><hr><h3 id="八、本章核心总结"><a href="#八、本章核心总结" class="headerlink" title="八、本章核心总结"></a>八、本章核心总结</h3><ul><li><strong>内存模型是理解 GC、性能调优、OOM 排查的基础</strong></li><li><strong>堆是 GC 主战场，方法区存储类元数据</strong></li><li><strong>对象生命周期 &#x3D; 创建 → 使用 → 回收</strong></li><li><strong>OOM 本质 &#x3D; 内存不足 or 对象无法释放（泄漏）</strong></li><li>**调优关键：合理设置堆、元空间、栈大小，选择合适 GC 算法</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>maxDepth</title>
      <link href="/2025/12/25/maxDepth/"/>
      <url>/2025/12/25/maxDepth/</url>
      
        <content type="html"><![CDATA[<p>给定一个二叉树 root ，返回其最大深度。</p><p>二叉树的 最大深度 是指从根节点到最远叶子节点的最长路径上的节点数。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 计算二叉树的最大深度</span></span><br><span class="line"><span class="comment">     * 最大深度是从根节点到最远叶子节点的最长路径上的节点数</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> root 二叉树的根节点</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 二叉树的最大深度</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">maxDepth</span><span class="params">(TreeNode root)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 递归计算左子树和右子树的最大深度</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">leftHeight</span> <span class="operator">=</span> maxDepth(root.left);</span><br><span class="line">            <span class="type">int</span> <span class="variable">rightHeight</span> <span class="operator">=</span> maxDepth(root.right);</span><br><span class="line">            <span class="comment">// 返回左右子树中较大的深度值加1（当前节点）</span></span><br><span class="line">            <span class="keyword">return</span> Math.max(leftHeight, rightHeight) + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>😂</p><p><img src="/../img/bored.jpg"></p>]]></content>
      
      
      
        <tags>
            
            <tag> algorithm </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>20230203_lost requst in asyn</title>
      <link href="/2023/02/03/20230203-lost-requst-in-asyn/"/>
      <url>/2023/02/03/20230203-lost-requst-in-asyn/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>springboot项目集成swagger的实现</title>
      <link href="/2022/11/21/221121spring/"/>
      <url>/2022/11/21/221121spring/</url>
      
        <content type="html"><![CDATA[<p>这几日在项目中，老是看到看到许多swagger注解，许多只能了解各大概，抱着得过且过的态度用用就行了。直到自己在构建项目时，发现单纯的添加依赖会报空指针错误。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Failed to start bean &#x27;documentationPluginsBootstrapper&#x27;; nested exception is java.lang.NullPointerException</span><br></pre></td></tr></table></figure><p>一番百度后发现，Springfox假设MVC的路径匹配政策是<code>ant-path-matcher</code>,而 Spring Boot 2.6.x版本的默认匹配策略是 <code>path-pattern-matcher</code>，这就造成了上面的报错。</p><h2 id="依赖"><a href="#依赖" class="headerlink" title="依赖"></a>依赖</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.springfox<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springfox-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="配置注解"><a href="#配置注解" class="headerlink" title="配置注解"></a>配置注解</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableSwagger2</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SwaggerConfig</span> &#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> Docket <span class="title function_">docket</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">ApiInfo</span> <span class="variable">apiInfo</span> <span class="operator">=</span></span><br><span class="line">                <span class="keyword">new</span> <span class="title class_">ApiInfoBuilder</span>()</span><br><span class="line">                        .title(<span class="string">&quot;5101Club日常管理系统API——Gateway&quot;</span>)</span><br><span class="line">                        .description(<span class="string">&quot;管理系统采用SpringBoot开发，API文档集成Swagger&quot;</span>)</span><br><span class="line">                        .version(<span class="string">&quot;1.0&quot;</span>)</span><br><span class="line">                        .contact(<span class="keyword">new</span> <span class="title class_">Contact</span>(<span class="string">&quot;slipperySoap&quot;</span>, <span class="string">&quot;https://github.com/XXDBOOM&quot;</span>, <span class="string">&quot;xxdboom@outlook.com&quot;</span>))</span><br><span class="line">                        .license(<span class="string">&quot;Apache 2.0&quot;</span>)</span><br><span class="line">                        .licenseUrl(<span class="string">&quot;http://www.apache.org/licenses/LICENSE-2.0&quot;</span>)</span><br><span class="line">                        .build();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Docket</span>(DocumentationType.SWAGGER_2)</span><br><span class="line">                .apiInfo(apiInfo)</span><br><span class="line">                .select()</span><br><span class="line">                <span class="comment">// 配置swagger显示的controller，如果不配置则默认扫描所有后端接口</span></span><br><span class="line">                .apis(RequestHandlerSelectors.basePackage(<span class="string">&quot;com.xxd.testdemo.controller&quot;</span>))</span><br><span class="line">                .paths(PathSelectors.any())</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">mvc:</span></span><br><span class="line">    <span class="attr">pathmatch:</span></span><br><span class="line">      <span class="attr">matching-strategy:</span> <span class="string">ant_path_matcher</span></span><br></pre></td></tr></table></figure><h2 id="运行测试"><a href="#运行测试" class="headerlink" title="运行测试"></a>运行测试</h2><div><p><img src="https://gitee.com/xxdboom/images/raw/master/blog/20221121200603.png" alt="result"></p></div>]]></content>
      
      
      
        <tags>
            
            <tag> spring </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>通过Feign调用外网HTTP，设置请求头，熔断器</title>
      <link href="/2022/11/14/221114cloud/"/>
      <url>/2022/11/14/221114cloud/</url>
      
        <content type="html"><![CDATA[<p>在许多项目中，往往需要和其他厂商的项目进行对接。这就不得不访问外部url接口。网上的各种方案层出不穷，但我不太喜欢额外添加maven依赖，所以使用已有的feign实现。</p><p>由于个人习惯，我的spring-boot版本是2.6.7。这导致了我添加openfeign依赖时总是报错，因为网络上的教程往往是使用了Hoxton.SR8版本的spring-cloud，它的绑定依赖openfeign是2.2.5.RELEASE。</p><p>版本兼容的问题往往最难为新手。</p><ol><li><p>添加Maven依赖</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-openfeign<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-loadbalancer<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-netflix-hystrix<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">version</span>&gt;</span>2.2.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><p>在启动类添加注解</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableFeignClients</span></span><br></pre></td></tr></table></figure></li><li><p>在配置文件中打开熔断器</p><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">feign.circuitbreaker.enabled</span>=<span class="string">true</span></span><br></pre></td></tr></table></figure></li><li><p>编写Feign接口</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient(url = &quot;http://aliyun.test.com&quot;, name = &quot;test&quot;, fallback = OutFeignImpl.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">OutFeign</span> &#123;</span><br><span class="line">    <span class="meta">@GetMapping(value = &quot;/test&quot;, headers = &#123;&quot;session_id=1001&quot;&#125;,produces = &#123;&quot;application/json&quot;&#125;)</span></span><br><span class="line">    Object <span class="title function_">getInfo</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol><li><p>在@FeignClient中，url指定外部http请求，name或value为注解必写参数，fallback则是熔断器指定实现类。</p></li><li><p>@GetMapping中，headers可以设置请求头，因不同于大部分博客上的版本，所以headers中的参数用等于号相连。</p></li></ol></li><li><p>编写熔断器实现类</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OutFeignImpl</span> <span class="keyword">implements</span> <span class="title class_">OutFeign</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">getInfo</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;---触发熔断---&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><p>至此，便可以在项目中调用外部url了。</p><p>参考：<a href="https://www.jb51.net/article/215784.htm">使用feign服务调用添加Header参数_java_脚本之家 (jb51.net)</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> openfeign </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Copy &#39;xxx&#39; to effectively final temp variable 问题解决</title>
      <link href="/2022/11/02/Copy%20&#39;xxx&#39;%20to%20effectively%20final%20temp%20variable%20%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3/"/>
      <url>/2022/11/02/Copy%20&#39;xxx&#39;%20to%20effectively%20final%20temp%20variable%20%E9%97%AE%E9%A2%98%E8%A7%A3%E5%86%B3/</url>
      
        <content type="html"><![CDATA[<p>今天在项目中修改接口的时候突然发现了类型问题，便用if函数对变量进行了类型的校验。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">if</span> (params.get(<span class="string">&quot;a&quot;</span>) <span class="keyword">instanceof</span> Integer) &#123;</span><br><span class="line">   a = (<span class="type">int</span>) params.get(<span class="string">&quot;a&quot;</span>);</span><br><span class="line">&#125;<span class="keyword">else</span> &#123;</span><br><span class="line">   a = Integer.parseInt(params.get(<span class="string">&quot;a&quot;</span>).toString());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>没有多想，便进行了提交，编译后发现竟然报错了。筛查后发现，在这块代码下有一处匿名内部类调用了该变量。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">finalA</span> <span class="operator">=</span> a;</span><br><span class="line"><span class="type">Thread</span> <span class="variable">thread</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;thread a = &quot;</span> + finalA);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>出错的原因是：内部类对象的生命周期会超过局部变量的生命周期。</p><p>所以在匿名内部类中，只能调用被final修饰的局部变量，而在java 8中，如果局部变量被匿名内部类访问，那么该变量会自动被final隐性修饰。</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
